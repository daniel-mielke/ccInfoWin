---
phase: 16-burn-rate-warning
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - CCInfoWindows/CCInfoWindows/Models/BurnRatePrediction.cs
  - CCInfoWindows/CCInfoWindows/Helpers/BurnRateCalculator.cs
  - CCInfoWindows/CCInfoWindows/Helpers/BurnRateFormatter.cs
  - CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml
  - CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw
  - CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw
  - CCInfoWindows.Tests/Helpers/BurnRateCalculatorTests.cs
  - CCInfoWindows.Tests/Helpers/BurnRateFormatterTests.cs
autonomous: true
requirements:
  - BURN-01
  - BURN-02
  - BURN-03
  - BURN-06
  - BURN-07

must_haves:
  truths:
    - "BurnRateCalculator.Predict returns a BurnRatePrediction when slope is positive and projected exhaustion is before resetsAt"
    - "BurnRateCalculator.Predict returns null when utilization < 20%, fewer than 3 data points, slope <= 0, or projected exhaustion is after resetsAt"
    - "BurnRatePrediction contains HitsLimitAt, MinutesUntilLimit (int, minimum 1), and is a plain data model with no formatting logic"
    - "BurnRateFormatter.FormatTimeLabel returns localized time strings using BurnRateFormat_* resource keys"
    - "BurnRateFormatter.FormatTimeLabel is the SINGLE source of hours/minutes formatting for both banner and toast (DRY)"
    - "Localization keys BurnRateBannerText, BurnRateFormat_HoursMinutes, BurnRateFormat_HoursOnly, BurnRateFormat_MinutesOnly, BurnRateNotificationTitle, BurnRateNotificationBody exist in both de-DE and en-US .resw files"
    - "BurnRateWarningBrush theme resource exists in both Dark (#FF453A) and Light (#FF3B30) dictionaries"
  artifacts:
    - path: "CCInfoWindows/CCInfoWindows/Models/BurnRatePrediction.cs"
      provides: "Plain data model for burn rate prediction result"
      contains: "class BurnRatePrediction"
    - path: "CCInfoWindows/CCInfoWindows/Helpers/BurnRateCalculator.cs"
      provides: "Static prediction engine with linear regression"
      exports: ["Predict"]
    - path: "CCInfoWindows/CCInfoWindows/Helpers/BurnRateFormatter.cs"
      provides: "Shared time-label formatting for burn rate (DRY extraction)"
      exports: ["FormatTimeLabel"]
    - path: "CCInfoWindows.Tests/Helpers/BurnRateCalculatorTests.cs"
      provides: "Unit tests for all BurnRateCalculator guard clauses and prediction cases"
      min_lines: 80
    - path: "CCInfoWindows.Tests/Helpers/BurnRateFormatterTests.cs"
      provides: "Unit tests for BurnRateFormatter time label formatting"
      min_lines: 30
    - path: "CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml"
      provides: "BurnRateWarningBrush in Dark and Light theme dictionaries"
      contains: "BurnRateWarningBrush"
    - path: "CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw"
      provides: "German burn rate localization strings"
      contains: "BurnRateBannerText"
    - path: "CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw"
      provides: "English burn rate localization strings"
      contains: "BurnRateBannerText"
  key_links:
    - from: "CCInfoWindows/CCInfoWindows/Helpers/BurnRateCalculator.cs"
      to: "CCInfoWindows/CCInfoWindows/Models/BurnRatePrediction.cs"
      via: "Predict returns BurnRatePrediction?"
      pattern: "BurnRatePrediction\\?"
    - from: "CCInfoWindows/CCInfoWindows/Helpers/BurnRateCalculator.cs"
      to: "CCInfoWindows/CCInfoWindows/Models/UsageHistory.cs"
      via: "IReadOnlyList<UsageHistoryPoint> parameter"
      pattern: "UsageHistoryPoint"
    - from: "CCInfoWindows/CCInfoWindows/Helpers/BurnRateFormatter.cs"
      to: "CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw"
      via: "Localizer.Get().GetLocalizedString for BurnRateFormat_* keys"
      pattern: "BurnRateFormat_"
    - from: "CCInfoWindows.Tests/Helpers/BurnRateCalculatorTests.cs"
      to: "CCInfoWindows/CCInfoWindows/Helpers/BurnRateCalculator.cs"
      via: "xUnit tests calling Predict()"
      pattern: "BurnRateCalculator\\.Predict"
    - from: "CCInfoWindows.Tests/Helpers/BurnRateFormatterTests.cs"
      to: "CCInfoWindows/CCInfoWindows/Helpers/BurnRateFormatter.cs"
      via: "xUnit tests calling FormatTimeLabel()"
      pattern: "BurnRateFormatter\\.FormatTimeLabel"
---

<objective>
Create the burn rate prediction engine, shared time formatter, data model, theme resources, localization strings, and comprehensive unit tests.

Purpose: Establish the pure-math prediction foundation (BurnRateCalculator), the shared time formatting helper (BurnRateFormatter), and all static resources (theme brushes, localization keys) that Plan 02 will wire into the UI and notification service.
Output: Tested BurnRateCalculator, tested BurnRateFormatter, BurnRatePrediction model, AppTheme.xaml brush, 6 localization keys in both languages, unit tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@CCInfoWindows/CCInfoWindows/Models/UsageHistory.cs
@CCInfoWindows/CCInfoWindows/Models/UsageData.cs
@CCInfoWindows/CCInfoWindows/Helpers/CountdownFormatter.cs
@CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml
@CCInfoWindows.Tests/Helpers/CountdownFormatterTests.cs
@spec/v1.10.0-macOS/spec-release-1.8.3-to-1.10.0.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From CCInfoWindows/CCInfoWindows/Models/UsageHistory.cs:
```csharp
// Utilization is stored as 0.0-1.0 (normalized), not the API's 0-100.
public class UsageHistoryPoint
{
    public DateTimeOffset Timestamp { get; set; }
    public double Utilization { get; set; }  // 0.0 - 1.0
}
```

From CCInfoWindows/CCInfoWindows/Models/UsageData.cs:
```csharp
public class UsageWindow
{
    public double Utilization { get; set; }           // 0-100 from API
    public double NormalizedUtilization => Utilization / 100.0;  // 0.0-1.0
    public DateTimeOffset? ResetsAt { get; set; }
}
```

From CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml (existing pattern):
```xml
<!-- Dark theme -->
<SolidColorBrush x:Key="ProgressRedBrush" Color="#FF453A" />
<!-- Light theme -->
<SolidColorBrush x:Key="ProgressRedBrush" Color="#FF3B30" />
```

From CCInfoWindows.Tests/Helpers/CountdownFormatterTests.cs (test pattern):
```csharp
using CCInfoWindows.Helpers;
namespace CCInfoWindows.Tests.Helpers;

public class CountdownFormatterTests
{
    [Fact]
    public void FormatCountdown_Null_ReturnsDash()
    {
        Assert.Equal("--", CountdownFormatter.FormatCountdown(null));
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BurnRatePrediction model + BurnRateCalculator + BurnRateFormatter + unit tests (TDD)</name>
  <files>
    CCInfoWindows/CCInfoWindows/Models/BurnRatePrediction.cs,
    CCInfoWindows/CCInfoWindows/Helpers/BurnRateCalculator.cs,
    CCInfoWindows/CCInfoWindows/Helpers/BurnRateFormatter.cs,
    CCInfoWindows.Tests/Helpers/BurnRateCalculatorTests.cs,
    CCInfoWindows.Tests/Helpers/BurnRateFormatterTests.cs
  </files>
  <read_first>
    CCInfoWindows/CCInfoWindows/Models/UsageHistory.cs,
    CCInfoWindows/CCInfoWindows/Models/UsageData.cs,
    CCInfoWindows/CCInfoWindows/Helpers/CountdownFormatter.cs,
    CCInfoWindows.Tests/Helpers/CountdownFormatterTests.cs,
    spec/v1.10.0-macOS/spec-release-1.8.3-to-1.10.0.md (lines 37-98 for FEAT-01a algorithm)
  </read_first>
  <behavior>
    RED phase - write these tests FIRST (all must fail initially):

    BurnRateCalculator tests:
    - Predict_NullResetsAt_ReturnsNull: history=any, util=50, resetsAt=null -> null
    - Predict_PastResetsAt_ReturnsNull: resetsAt=UtcNow-1h -> null
    - Predict_LowUtilization_ReturnsNull: util=15 (below MinimumUtilization=20) -> null
    - Predict_TooFewPoints_ReturnsNull: 2 points in 15min window -> null
    - Predict_FlatUsage_ReturnsNull: 4 points all at 0.50 utilization (slope~0) -> null
    - Predict_DecreasingUsage_ReturnsNull: slope negative (0.80 -> 0.60) -> null
    - Predict_ExhaustsAfterReset_ReturnsNull: slow burn, resetsAt in 10min, projected at 60min -> null
    - Predict_FastBurn_ReturnsPrediction: 4 points going 0.20->0.40->0.50->0.60 in 10min, resetsAt in 3h -> non-null, MinutesUntilLimit roughly 25-35
    - Predict_MinutesUntilLimit_MinimumOne: edge case where remaining seconds < 60 -> MinutesUntilLimit == 1
    - Predict_FullUtilization_ReturnsNull: currentUtilization=100 (no remaining) -> null (100-100=0 seconds)

    BurnRateFormatter tests:
    - FormatTimeLabel_MinutesOnly_ReturnsMinutes: 33 -> uses BurnRateFormat_MinutesOnly key
    - FormatTimeLabel_HoursAndMinutes_ReturnsFormatted: 93 (1h33m) -> uses BurnRateFormat_HoursMinutes key
    - FormatTimeLabel_ExactHours_ReturnsHoursOnly: 120 (2h) -> uses BurnRateFormat_HoursOnly key
    - FormatTimeLabel_OneMinute_ReturnsMinutes: 1 -> uses BurnRateFormat_MinutesOnly key
    - FormatTimeLabel_SixtyMinutes_ReturnsOneHour: 60 -> uses BurnRateFormat_HoursOnly key
  </behavior>
  <action>
    **RED: Write tests first.**

    1. Create `CCInfoWindows.Tests/Helpers/BurnRateCalculatorTests.cs`:
       - `using CCInfoWindows.Helpers;` and `using CCInfoWindows.Models;`
       - Namespace: `CCInfoWindows.Tests.Helpers`
       - xUnit `[Fact]` tests per the behavior list above
       - For points: create `List<UsageHistoryPoint>` with `Timestamp` and `Utilization` (0.0-1.0 scale, as stored)
       - For `currentUtilization`: pass 0-100 scale (API value, NOT normalized)
       - For `resetsAt`: use `DateTimeOffset.UtcNow.AddHours(3)` or similar future timestamps
       - Helper method `MakePoint(DateTimeOffset ts, double utilNormalized)` to reduce test boilerplate

    2. Create `CCInfoWindows.Tests/Helpers/BurnRateFormatterTests.cs`:
       - `using CCInfoWindows.Helpers;`
       - Namespace: `CCInfoWindows.Tests.Helpers`
       - NOTE: BurnRateFormatter uses `Localizer.Get().GetLocalizedString()` which requires WinUI3Localizer initialization. Since this is a unit test without WinUI runtime, use the overload that accepts a formatting delegate OR structure the class so time formatting logic is testable without localization.
       - **Recommended approach:** BurnRateFormatter has TWO methods:
         - `public static string FormatTimeLabel(int minutes)` — the localized version for production code, calls Localizer internally
         - `internal static (int hours, int remainingMinutes, TimeFormat format) ParseTime(int minutes)` — pure logic that returns an enum `{ MinutesOnly, HoursOnly, HoursMinutes }` + the extracted h/m values
       - Test `ParseTime` in unit tests (no Localizer dependency):
         - `ParseTime(33)` -> `(0, 33, TimeFormat.MinutesOnly)`
         - `ParseTime(93)` -> `(1, 33, TimeFormat.HoursMinutes)`
         - `ParseTime(120)` -> `(2, 0, TimeFormat.HoursOnly)`
         - `ParseTime(1)` -> `(0, 1, TimeFormat.MinutesOnly)`
         - `ParseTime(60)` -> `(1, 0, TimeFormat.HoursOnly)`

    3. Create `CCInfoWindows/CCInfoWindows/Models/BurnRatePrediction.cs`:
       - Namespace: `CCInfoWindows.Models`
       - Plain data class (NOT record, to match project pattern of class-based models)
       - Properties: `DateTimeOffset HitsLimitAt { get; set; }`, `int MinutesUntilLimit { get; set; }`
       - NO `FormattedTimeUntilLimit` -- formatting happens via BurnRateFormatter (keeps model as plain data)

    4. Create `CCInfoWindows/CCInfoWindows/Helpers/BurnRateFormatter.cs`:
       - Namespace: `CCInfoWindows.Helpers`
       - `public static class BurnRateFormatter`
       - `using WinUI3Localizer;`
       - Internal enum: `internal enum TimeFormat { MinutesOnly, HoursOnly, HoursMinutes }`
       - `internal static (int hours, int remainingMinutes, TimeFormat format) ParseTime(int minutes)`:
         - If `minutes >= 60`: `h = minutes / 60`, `m = minutes % 60`
           - If `m == 0`: return `(h, 0, TimeFormat.HoursOnly)`
           - Else: return `(h, m, TimeFormat.HoursMinutes)`
         - Else: return `(0, minutes, TimeFormat.MinutesOnly)`
       - `public static string FormatTimeLabel(int minutes)`:
         - Call `ParseTime(minutes)` to get `(h, m, format)`
         - Switch on format:
           - `TimeFormat.HoursOnly`: `string.Format(Localizer.Get().GetLocalizedString("BurnRateFormat_HoursOnly"), h)`
           - `TimeFormat.HoursMinutes`: `string.Format(Localizer.Get().GetLocalizedString("BurnRateFormat_HoursMinutes"), h, m)`
           - `TimeFormat.MinutesOnly`: `string.Format(Localizer.Get().GetLocalizedString("BurnRateFormat_MinutesOnly"), m)`
       - This is the SINGLE source of time formatting for both BurnRateNotificationService and MainViewModel (DRY).
       - IMPORTANT: `[assembly: InternalsVisibleTo("CCInfoWindows.Tests")]` must be added to an AssemblyInfo.cs or to the main project's .csproj `<InternalsVisibleTo>` element so that `ParseTime` and `TimeFormat` are accessible from tests.

    5. Create `CCInfoWindows/CCInfoWindows/Helpers/BurnRateCalculator.cs`:
       - Namespace: `CCInfoWindows.Helpers`
       - `public static class BurnRateCalculator`
       - Named constants (NO magic numbers):
         ```
         private const double MinimumUtilization = 20.0;
         private const int LookbackWindowMinutes = 15;
         private const int MinimumDataPoints = 3;
         private const double MaxUtilization = 100.0;
         ```
       - `public static BurnRatePrediction? Predict(IReadOnlyList<UsageHistoryPoint> history, double currentUtilization, DateTimeOffset? resetsAt)`
       - Guard 1: `resetsAt` null or in the past -> return null
       - Guard 2: `currentUtilization < MinimumUtilization` -> return null
       - Guard 3: filter history to last 15 minutes (`DateTimeOffset.UtcNow - TimeSpan.FromMinutes(LookbackWindowMinutes)`), if count < MinimumDataPoints -> return null
       - CRITICAL: When iterating history points for regression, multiply `p.Utilization * 100.0` to convert from 0-1 (stored) to 0-100 (algorithm scale)
       - Linear regression: x = seconds since first point, y = utilization * 100
         - `slope = (n * sumXY - sumX * sumY) / (n * sumX2 - sumX * sumX)`
         - Guard denominator near zero -> return null
       - Guard 4: `slope <= 0` -> return null
       - Project: `secondsToLimit = (MaxUtilization - currentUtilization) / slope`
       - Guard 5: `currentUtilization >= MaxUtilization` -> return null (no room left, secondsToLimit=0)
       - `hitsLimitAt = DateTimeOffset.UtcNow.AddSeconds(secondsToLimit)`
       - Guard 6: `hitsLimitAt >= resetsAt.Value` -> return null
       - `minutesUntilLimit = Math.Max(1, (int)Math.Floor(secondsToLimit / 60.0))`
       - Return `new BurnRatePrediction { HitsLimitAt = hitsLimitAt, MinutesUntilLimit = minutesUntilLimit }`

    **GREEN: Run tests, all should pass.**

    **REFACTOR: Clean up if needed, rerun tests.**
  </action>
  <verify>
    <automated>dotnet test CCInfoWindows.Tests/CCInfoWindows.Tests.csproj --filter "FullyQualifiedName~BurnRateCalculator|FullyQualifiedName~BurnRateFormatter" --no-restore</automated>
  </verify>
  <acceptance_criteria>
    - CCInfoWindows/CCInfoWindows/Models/BurnRatePrediction.cs contains `public class BurnRatePrediction`
    - CCInfoWindows/CCInfoWindows/Models/BurnRatePrediction.cs contains `public DateTimeOffset HitsLimitAt`
    - CCInfoWindows/CCInfoWindows/Models/BurnRatePrediction.cs contains `public int MinutesUntilLimit`
    - CCInfoWindows/CCInfoWindows/Models/BurnRatePrediction.cs does NOT contain `FormattedTimeUntilLimit`
    - CCInfoWindows/CCInfoWindows/Helpers/BurnRateCalculator.cs contains `public static class BurnRateCalculator`
    - CCInfoWindows/CCInfoWindows/Helpers/BurnRateCalculator.cs contains `public static BurnRatePrediction? Predict(`
    - CCInfoWindows/CCInfoWindows/Helpers/BurnRateCalculator.cs contains `private const double MinimumUtilization = 20.0`
    - CCInfoWindows/CCInfoWindows/Helpers/BurnRateCalculator.cs contains `private const int LookbackWindowMinutes = 15`
    - CCInfoWindows/CCInfoWindows/Helpers/BurnRateCalculator.cs contains `private const int MinimumDataPoints = 3`
    - CCInfoWindows/CCInfoWindows/Helpers/BurnRateCalculator.cs contains `p.Utilization * 100.0` (critical 0-1 to 0-100 conversion)
    - CCInfoWindows/CCInfoWindows/Helpers/BurnRateFormatter.cs contains `public static class BurnRateFormatter`
    - CCInfoWindows/CCInfoWindows/Helpers/BurnRateFormatter.cs contains `public static string FormatTimeLabel(int minutes)`
    - CCInfoWindows/CCInfoWindows/Helpers/BurnRateFormatter.cs contains `internal static` method `ParseTime`
    - CCInfoWindows/CCInfoWindows/Helpers/BurnRateFormatter.cs contains `TimeFormat` enum
    - CCInfoWindows.Tests/Helpers/BurnRateCalculatorTests.cs contains at least 10 `[Fact]` methods
    - CCInfoWindows.Tests/Helpers/BurnRateFormatterTests.cs contains at least 5 `[Fact]` methods testing ParseTime
    - `dotnet test --filter "BurnRateCalculator"` exits 0 with all tests passing
    - `dotnet test --filter "BurnRateFormatter"` exits 0 with all tests passing
  </acceptance_criteria>
  <done>BurnRateCalculator.Predict returns correct predictions for increasing usage and null for all guard conditions. BurnRateFormatter.FormatTimeLabel is the single shared source of hours/minutes formatting (DRY). All 10+ calculator tests and 5+ formatter tests pass green. BurnRatePrediction is a plain data model with HitsLimitAt and MinutesUntilLimit.</done>
</task>

<task type="auto">
  <name>Task 2: Theme brush + localization strings for burn rate</name>
  <files>
    CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml,
    CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw,
    CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw
  </files>
  <read_first>
    CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml,
    CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw,
    CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw
  </read_first>
  <action>
    1. **AppTheme.xaml** -- Add `BurnRateWarningBrush` to BOTH theme dictionaries, following the exact existing pattern of `ProgressRedBrush`:

       In the Dark `ResourceDictionary` (x:Key="Dark"), add after the `ShimmerHighlightBrush` line:
       ```xml
       <SolidColorBrush x:Key="BurnRateWarningBrush" Color="#FF453A" />
       ```

       In the Light `ResourceDictionary` (x:Key="Light"), add after the `ShimmerHighlightBrush` line:
       ```xml
       <SolidColorBrush x:Key="BurnRateWarningBrush" Color="#FF3B30" />
       ```

       No `BurnRateWarningTextBrush` needed -- text is hardcoded White in both themes.

    2. **Strings/de-DE/Resources.resw** -- Add 6 new entries at the end of the file (before the closing `</root>` tag). These are code-behind keys accessed via `Localizer.Get().GetLocalizedString()`, NOT Uid-targeted, so they use plain `name` without `.Text` suffix:

       ```xml
       <data name="BurnRateBannerText" xml:space="preserve">
         <value>Token-Limit erreicht in {0}</value>
       </data>
       <data name="BurnRateFormat_HoursMinutes" xml:space="preserve">
         <value>~{0}h {1}min</value>
       </data>
       <data name="BurnRateFormat_HoursOnly" xml:space="preserve">
         <value>~{0}h</value>
       </data>
       <data name="BurnRateFormat_MinutesOnly" xml:space="preserve">
         <value>~{0}min</value>
       </data>
       <data name="BurnRateNotificationTitle" xml:space="preserve">
         <value>Burn-Rate-Warnung</value>
       </data>
       <data name="BurnRateNotificationBody" xml:space="preserve">
         <value>Bei aktuellem Tempo wird das Token-Limit in {0} erreicht.</value>
       </data>
       ```

    3. **Strings/en-US/Resources.resw** -- Add the same 6 keys with English values:

       ```xml
       <data name="BurnRateBannerText" xml:space="preserve">
         <value>Token limit reached in {0}</value>
       </data>
       <data name="BurnRateFormat_HoursMinutes" xml:space="preserve">
         <value>~{0}h {1}min</value>
       </data>
       <data name="BurnRateFormat_HoursOnly" xml:space="preserve">
         <value>~{0}h</value>
       </data>
       <data name="BurnRateFormat_MinutesOnly" xml:space="preserve">
         <value>~{0}min</value>
       </data>
       <data name="BurnRateNotificationTitle" xml:space="preserve">
         <value>Burn rate warning</value>
       </data>
       <data name="BurnRateNotificationBody" xml:space="preserve">
         <value>At current pace, token limit reached in {0}.</value>
       </data>
       ```
  </action>
  <verify>
    <automated>dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj</automated>
  </verify>
  <acceptance_criteria>
    - CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml contains `x:Key="BurnRateWarningBrush" Color="#FF453A"` in Dark section
    - CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml contains `x:Key="BurnRateWarningBrush" Color="#FF3B30"` in Light section
    - CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw contains `name="BurnRateBannerText"`
    - CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw contains `Token-Limit erreicht in {0}`
    - CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw contains `name="BurnRateFormat_HoursMinutes"`
    - CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw contains `name="BurnRateNotificationTitle"`
    - CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw contains `Burn-Rate-Warnung`
    - CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw contains `name="BurnRateNotificationBody"`
    - CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw contains `name="BurnRateBannerText"`
    - CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw contains `Token limit reached in {0}`
    - CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw contains `Burn rate warning`
    - CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw contains `At current pace, token limit reached in {0}.`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>AppTheme.xaml has BurnRateWarningBrush in both Dark (#FF453A) and Light (#FF3B30) dictionaries. Both de-DE and en-US .resw files have all 6 burn rate localization keys with correct values.</done>
</task>

</tasks>

<verification>
1. `dotnet test CCInfoWindows.Tests/CCInfoWindows.Tests.csproj --filter "FullyQualifiedName~BurnRateCalculator"` -- all burn rate calculator tests pass
2. `dotnet test CCInfoWindows.Tests/CCInfoWindows.Tests.csproj --filter "FullyQualifiedName~BurnRateFormatter"` -- all burn rate formatter tests pass
3. `dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj` -- project builds with new theme resources and localization
4. Grep `BurnRateWarningBrush` in AppTheme.xaml -- found twice (Dark + Light)
5. Grep `BurnRateBannerText` in both .resw files -- found in both
6. Grep `FormatTimeLabel` in BurnRateFormatter.cs -- single source of time formatting
</verification>

<success_criteria>
- BurnRateCalculator.Predict correctly returns BurnRatePrediction for increasing usage and null for all 6 guard conditions
- BurnRateFormatter.FormatTimeLabel is the single shared time formatting method (no duplicated formatting in other classes)
- All 10+ calculator unit tests pass
- All 5+ formatter unit tests pass
- BurnRateWarningBrush exists in both theme dictionaries with correct hex colors
- All 6 localization keys exist in both de-DE and en-US with correct translations
- Project builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/16-burn-rate-warning/16-01-SUMMARY.md`
</output>
