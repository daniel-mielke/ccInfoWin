---
phase: 09-layout-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CCInfoWindows/CCInfoWindows/Views/MainView.xaml
  - CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw
  - CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-04]

must_haves:
  truths:
    - "User sees equal padding on all four sides of the app window"
    - "User sees a localized ACTIVE SESSION / AKTIVE SITZUNG header above the session dropdown"
    - "User sees a horizontal separator below the session dropdown area"
    - "User sees the Context Window section between Active Session and 5-Hour Window"
  artifacts:
    - path: "CCInfoWindows/CCInfoWindows/Views/MainView.xaml"
      provides: "Restructured layout with new section order, header, and separators"
      contains: "SectionHeaderActiveSession"
    - path: "CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw"
      provides: "English localization for Active Session header"
      contains: "SectionHeaderActiveSession.Text"
    - path: "CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw"
      provides: "German localization for Active Session header"
      contains: "SectionHeaderActiveSession.Text"
  key_links:
    - from: "MainView.xaml"
      to: "Resources.resw"
      via: "l:Uids.Uid=\"SectionHeaderActiveSession\""
      pattern: "l:Uids.Uid=\"SectionHeaderActiveSession\""
---

<objective>
Restructure MainView.xaml section order: add "Active Session" header, equalize padding, insert separator below dropdown, and relocate the Context Window section to appear between Active Session and 5-Hour Window.

Purpose: Users see a visually consistent layout with the correct section ordering per the macOS reference app.
Output: Updated MainView.xaml with new section order, updated localization files with new key.
</objective>

<execution_context>
@C:/Users/DanielMielke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DanielMielke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-layout-structure/09-CONTEXT.md
@.planning/phases/09-layout-structure/09-RESEARCH.md

<interfaces>
<!-- No code interfaces needed — pure XAML restructuring using established patterns -->

Existing separator pattern (used 4x in MainView.xaml):
```xml
<Border Height="1" Background="{ThemeResource DividerBrush}" />
```

Existing section header pattern:
```xml
<TextBlock l:Uids.Uid="SectionHeaderContextWindow"
           FontSize="11" FontWeight="SemiBold"
           Foreground="{ThemeResource SectionHeaderBrush}"
           CharacterSpacing="50" />
```

Existing .resw key pattern:
```xml
<data name="SectionHeaderContextWindow.Text" xml:space="preserve">
  <value>CONTEXT WINDOW</value>
</data>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add localization keys and equalize padding</name>
  <files>
    CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw,
    CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw,
    CCInfoWindows/CCInfoWindows/Views/MainView.xaml
  </files>
  <read_first>
    CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw,
    CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw,
    CCInfoWindows/CCInfoWindows/Views/MainView.xaml
  </read_first>
  <action>
1. In `en-US/Resources.resw`: Insert the following new data element within the "MainView section headers" comment block (after the existing `SectionHeaderStats.Text` entry, before the `<!-- MainView body text -->` comment):
```xml
<data name="SectionHeaderActiveSession.Text" xml:space="preserve">
  <value>ACTIVE SESSION</value>
</data>
```

2. In `de-DE/Resources.resw`: Insert the same key in the same location:
```xml
<data name="SectionHeaderActiveSession.Text" xml:space="preserve">
  <value>AKTIVE SITZUNG</value>
</data>
```

3. In `MainView.xaml` line 30: Change `Padding="16,12,16,12"` to `Padding="16,16,16,16"` on the root Grid element.
  </action>
  <verify>
    <automated>dotnet build D:/myProjects/ccInfoWin/CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj</automated>
  </verify>
  <acceptance_criteria>
    - en-US/Resources.resw contains `<data name="SectionHeaderActiveSession.Text" xml:space="preserve">` with value `ACTIVE SESSION`
    - de-DE/Resources.resw contains `<data name="SectionHeaderActiveSession.Text" xml:space="preserve">` with value `AKTIVE SITZUNG`
    - MainView.xaml contains `Padding="16,16,16,16"` on the root Grid
    - MainView.xaml does NOT contain `Padding="16,12,16,12"`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Localization keys exist in both .resw files, root Grid has equal 16px padding on all sides</done>
</task>

<task type="auto">
  <name>Task 2: Add Active Session header, separator, and relocate Context Window section</name>
  <files>CCInfoWindows/CCInfoWindows/Views/MainView.xaml</files>
  <read_first>CCInfoWindows/CCInfoWindows/Views/MainView.xaml</read_first>
  <action>
This task performs three coordinated edits to MainView.xaml. The file must be re-read after Task 1 to see current line numbers.

**Edit A: Insert Active Session header before the ComboBox**

Insert the following TextBlock BEFORE the `<!-- ==================== SESSION DROPDOWN ==================== -->` comment and the `<ComboBox x:Name="SessionComboBox"` element, as a new child of the ScrollViewer's inner StackPanel:

```xml
<!-- ==================== AKTIVE SITZUNG ==================== -->
<TextBlock l:Uids.Uid="SectionHeaderActiveSession"
           FontSize="11" FontWeight="SemiBold"
           Foreground="{ThemeResource SectionHeaderBrush}"
           CharacterSpacing="50" />
```

**Edit B: Insert separator after the updating indicator**

After the `<!-- "Aktualisiere..." indicator on startup -->` TextBlock (the one with `l:Uids.Uid="UpdatingIndicator"`), insert:

```xml
<!-- Divider below Active Session dropdown -->
<Border Height="1" Background="{ThemeResource DividerBrush}" />
```

This separator goes AFTER the scanning indicator and updating indicator TextBlocks, BEFORE the Context Window section.

**Edit C: Move the entire Context Window block up**

Cut the following block (currently between the Sonnet Weekly section and the Statistics section):
- The `<!-- Divider before KONTEXTFENSTER -->` Border (line 309-310 area)
- The entire `<!-- ==================== KONTEXTFENSTER ==================== -->` StackPanel (lines 312-409 area)

Paste this block AFTER the new separator from Edit B and BEFORE the `<!-- ==================== 5-STUNDEN-FENSTER ==================== -->` section.

The existing `<!-- Divider before STATISTIKEN -->` Border (line 412 area) stays in its relative position — it will now serve as the separator between the Context Window section and the 5-Hour Window section.

**IMPORTANT:** After the move, add a new divider between the Context Window section and the 5-Hour Window section if the existing "Divider before STATISTIKEN" is no longer between them. The target structure must be:

```
Active Session header
ComboBox
Scanning indicator
Updating indicator
[separator - NEW from Edit B]
Context Window StackPanel
[separator - between context and 5-hour]
5-Hour Window StackPanel
[separator]
Weekly StackPanel
[conditional separator]
Sonnet Weekly StackPanel
[separator - "Divider before STATISTIKEN"]
Statistics StackPanel
```

The "Divider before KONTEXTFENSTER" that was cut moves WITH the Context Window block and becomes the separator between the new Edit B separator and the Context Window header — but since both would be adjacent, REMOVE the "Divider before KONTEXTFENSTER" Border entirely (it is redundant with the Edit B separator). Instead, ensure a separator exists AFTER the Context Window StackPanel and BEFORE the 5-Hour Window StackPanel. Use the standard pattern:
```xml
<!-- Divider after Context Window -->
<Border Height="1" Background="{ThemeResource DividerBrush}" />
```
  </action>
  <verify>
    <automated>dotnet build D:/myProjects/ccInfoWin/CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj</automated>
  </verify>
  <acceptance_criteria>
    - MainView.xaml contains `l:Uids.Uid="SectionHeaderActiveSession"` BEFORE `x:Name="SessionComboBox"`
    - MainView.xaml contains `SectionHeaderActiveSession` TextBlock with attributes: `FontSize="11"`, `FontWeight="SemiBold"`, `Foreground="{ThemeResource SectionHeaderBrush}"`, `CharacterSpacing="50"`
    - In MainView.xaml, the element order within the ScrollViewer StackPanel is: SectionHeaderActiveSession ... SessionComboBox ... ScanningIndicator ... UpdatingIndicator ... DividerBrush Border ... SectionHeaderContextWindow ... SectionHeaderFiveHour
    - The "Divider before KONTEXTFENSTER" comment/Border no longer exists as a standalone element between Sonnet Weekly and Context Window
    - A separator Border with DividerBrush exists between the Context Window StackPanel closing tag and the 5-Hour Window StackPanel opening tag
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Active Session header appears above dropdown, separator below dropdown area, Context Window section repositioned between Active Session and 5-Hour Window with separators above and below</done>
</task>

</tasks>

<verification>
1. `dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj` compiles without errors
2. `dotnet test CCInfoWindows.Tests/CCInfoWindows.Tests.csproj --arch x64` passes (no regressions)
3. In MainView.xaml, section order is: Active Session header > ComboBox > indicators > [separator] > Context Window > [separator] > 5-Hour > [separator] > Weekly > Sonnet Weekly > [separator] > Statistics
</verification>

<success_criteria>
- Root Grid Padding is "16,16,16,16" (LAYOUT-01)
- "SectionHeaderActiveSession" TextBlock exists above ComboBox with correct styling (LAYOUT-02)
- Separator Border exists below the updating indicator, before Context Window (LAYOUT-03)
- Context Window section appears before 5-Hour Window in the StackPanel child order (LAYOUT-04)
- Both .resw files contain "SectionHeaderActiveSession.Text" with correct values
- Build succeeds, all existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-layout-structure/09-01-SUMMARY.md`
</output>
