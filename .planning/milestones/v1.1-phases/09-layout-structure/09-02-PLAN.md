---
phase: 09-layout-structure
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - CCInfoWindows/CCInfoWindows/Views/MainView.xaml
autonomous: false
requirements: [LAYOUT-05, LAYOUT-06]

must_haves:
  truths:
    - "User can scroll down to reach the footer buttons (footer is not fixed to the bottom)"
    - "User sees a horizontal separator above the footer buttons"
    - "User sees a horizontal separator between the Models row and the Input row in Statistics"
  artifacts:
    - path: "CCInfoWindows/CCInfoWindows/Views/MainView.xaml"
      provides: "Footer inside ScrollViewer, statistics separator"
      contains: "Grid.ColumnSpan=\"2\" Height=\"1\" Margin=\"0,2,0,2\""
  key_links:
    - from: "MainView.xaml root Grid"
      to: "ScrollViewer StackPanel"
      via: "Footer StackPanel moved from Grid.Row=2 into ScrollViewer content"
      pattern: "RowDefinitions=\"Auto,\\*\""
---

<objective>
Move the footer from a fixed Grid row into the scrollable content area, and add a separator between Models and Input rows in the Statistics grid.

Purpose: Footer scrolls with content (not sticky), and Statistics section has a clear visual break between the model info row and the token data rows.
Output: Updated MainView.xaml with relocated footer and new statistics separator.
</objective>

<execution_context>
@C:/Users/DanielMielke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DanielMielke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-layout-structure/09-CONTEXT.md
@.planning/phases/09-layout-structure/09-RESEARCH.md
@.planning/phases/09-layout-structure/09-01-SUMMARY.md

<interfaces>
<!-- Current Statistics Grid structure (Grid.Row values before this plan): -->
<!-- Row 0: StatsModels (label + value + shimmer) -->
<!-- Row 1: StatsInput (label + value + shimmer) -->
<!-- Row 2: StatsOutput (label + value + shimmer) -->
<!-- Row 3: StatsCacheWrite (label + value + shimmer) -->
<!-- Row 4: StatsCacheRead (label + value + shimmer) -->
<!-- Row 5: Divider Border (Grid.ColumnSpan="2") -->
<!-- Row 6: StatsTotal (label + value + shimmer) -->
<!-- Row 7: StatsCost (label + value + shimmer) -->
<!-- Total: 8 RowDefinitions, all Height="Auto" -->

<!-- Footer StackPanel (currently Grid.Row="2"): -->
<!-- Contains: Refresh button (with spinner), Settings button, Quit button -->
<!-- Padding="0,8,0,4", Orientation="Horizontal", HorizontalAlignment="Center", Spacing="16" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move footer into ScrollViewer and collapse Grid rows</name>
  <files>CCInfoWindows/CCInfoWindows/Views/MainView.xaml</files>
  <read_first>CCInfoWindows/CCInfoWindows/Views/MainView.xaml</read_first>
  <action>
Three coordinated edits to MainView.xaml:

**Edit A: Change Grid RowDefinitions**

On the root Grid element, change `RowDefinitions="Auto,*,Auto"` to `RowDefinitions="Auto,*"`.

Note: After Plan 01, this should already be `RowDefinitions="Auto,*,Auto"` — if Plan 01 already changed it, verify current state before editing.

**Edit B: Cut the footer StackPanel from Grid.Row="2"**

Cut the entire footer StackPanel block — the element starting with `<StackPanel Grid.Row="2" Orientation="Horizontal"` through its closing `</StackPanel>` tag (currently the last child of the root Grid, after the ScrollViewer closing tag). This block contains:
- The refresh button Grid (with SpinnerStoryboard target RefreshIconTransform)
- The settings button
- The quit button

**Edit C: Paste footer as last child of ScrollViewer's inner StackPanel**

Insert the following at the END of the ScrollViewer's inner `<StackPanel Spacing="16">`, AFTER the Statistics StackPanel (the last content section):

1. A footer separator:
```xml
<!-- Divider above footer -->
<Border Height="1" Background="{ThemeResource DividerBrush}" Margin="0,4,0,0" />
```

2. The footer StackPanel (pasted from Edit B), but with the `Grid.Row="2"` attribute REMOVED. The element should read:
```xml
<StackPanel Orientation="Horizontal"
            HorizontalAlignment="Center" Spacing="16"
            Padding="0,8,0,4">
```
(All child elements inside remain unchanged.)

**CRITICAL:** Do NOT remove or change the `x:Name="RefreshIcon"`, `x:Name="RefreshIconTransform"`, or any Storyboard target references. The SpinnerStoryboard in Page.Resources still needs these names.
  </action>
  <verify>
    <automated>dotnet build D:/myProjects/ccInfoWin/CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj</automated>
  </verify>
  <acceptance_criteria>
    - MainView.xaml root Grid contains `RowDefinitions="Auto,*"` (exactly 2 rows, NOT 3)
    - MainView.xaml does NOT contain `Grid.Row="2"` on any StackPanel element
    - The footer StackPanel (containing `FooterRefreshButton`, `FooterSettingsButton`, `FooterQuitButton`) is inside the ScrollViewer's StackPanel, NOT a direct child of the root Grid
    - A Border with `Background="{ThemeResource DividerBrush}" Margin="0,4,0,0"` exists immediately before the footer StackPanel
    - The footer StackPanel has `Orientation="Horizontal"` and `Padding="0,8,0,4"` (unchanged)
    - `x:Name="RefreshIcon"` and `x:Name="RefreshIconTransform"` still exist in the footer
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Footer is inside ScrollViewer content, separator above footer visible, Grid collapsed to 2 rows</done>
</task>

<task type="auto">
  <name>Task 2: Insert separator between Models and Input rows in Statistics grid</name>
  <files>CCInfoWindows/CCInfoWindows/Views/MainView.xaml</files>
  <read_first>CCInfoWindows/CCInfoWindows/Views/MainView.xaml</read_first>
  <action>
Insert a new separator row between the Models row (Row 0) and the Input row (Row 1) in the Statistics data Grid. This requires:

**Edit A: Add a new RowDefinition**

In the Statistics Grid's `<Grid.RowDefinitions>` block, add one new `<RowDefinition Height="Auto" />` as the SECOND entry (index 1). The block should go from 8 to 9 RowDefinition entries total.

**Edit B: Insert the separator Border element**

After the Row 0 elements (StatsModels label, value, and shimmer — the three elements with `Grid.Row="0"`), insert:

```xml
<!-- Divider between Models and Input -->
<Border Grid.Row="1" Grid.ColumnSpan="2" Height="1" Margin="0,2,0,2"
        Background="{ThemeResource DividerBrush}" />
```

**Edit C: Increment Grid.Row on ALL elements currently at rows 1-7**

Every element in the Statistics Grid that has `Grid.Row="1"` through `Grid.Row="7"` must have its Grid.Row value incremented by 1. The complete mapping:

| Element | Old Grid.Row | New Grid.Row |
|---------|-------------|-------------|
| StatsModels elements (3) | 0 | 0 (unchanged) |
| NEW separator | — | 1 |
| StatsInput elements (3) | 1 | 2 |
| StatsOutput elements (3) | 2 | 3 |
| StatsCacheWrite elements (3) | 3 | 4 |
| StatsCacheRead elements (3) | 4 | 5 |
| Existing divider Border | 5 | 6 |
| StatsTotal elements (3) | 6 | 7 |
| StatsCost elements (3) | 7 | 8 |

That is 21 elements total that need Grid.Row updates (7 groups x 3 elements each). Do NOT miss any. Each "group" has: a label TextBlock, a value TextBlock, and a shimmer Border.

**Verification approach:** After editing, count that the Statistics Grid has exactly 9 RowDefinition entries and that Grid.Row values go 0, 1 (separator), 2, 3, 4, 5, 6 (existing divider), 7, 8.
  </action>
  <verify>
    <automated>dotnet build D:/myProjects/ccInfoWin/CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj</automated>
  </verify>
  <acceptance_criteria>
    - Statistics Grid has exactly 9 `<RowDefinition Height="Auto" />` entries
    - A Border with `Grid.Row="1" Grid.ColumnSpan="2" Height="1" Margin="0,2,0,2"` and `DividerBrush` exists in the Statistics Grid
    - StatsInput elements have `Grid.Row="2"` (not `Grid.Row="1"`)
    - StatsOutput elements have `Grid.Row="3"` (not `Grid.Row="2"`)
    - StatsCacheWrite elements have `Grid.Row="4"` (not `Grid.Row="3"`)
    - StatsCacheRead elements have `Grid.Row="5"` (not `Grid.Row="4"`)
    - Existing divider Border has `Grid.Row="6"` (not `Grid.Row="5"`)
    - StatsTotal elements have `Grid.Row="7"` (not `Grid.Row="6"`)
    - StatsCost elements have `Grid.Row="8"` (not `Grid.Row="7"`)
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Separator visible between Models and Input rows, all Grid.Row indices correctly shifted, no visual overlap</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all 6 LAYOUT changes</name>
  <files>CCInfoWindows/CCInfoWindows/Views/MainView.xaml</files>
  <action>Launch the app for visual verification of all layout changes from Plans 01 and 02.</action>
  <what-built>Complete layout restructuring: equal padding, Active Session header, separators, Context Window repositioned, scrollable footer, Statistics Models/Input separator</what-built>
  <how-to-verify>
    1. Launch the app: `dotnet run --project CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj`
    2. Verify LAYOUT-01: Top/bottom padding visually matches left/right padding (all 16px)
    3. Verify LAYOUT-02: "ACTIVE SESSION" (or "AKTIVE SITZUNG" in DE) label appears above the session dropdown, styled as uppercase small text
    4. Verify LAYOUT-03: A thin horizontal line appears below the dropdown/indicators area
    5. Verify LAYOUT-04: Context Window section (progress bar + model badge) appears BETWEEN Active Session and 5-Hour Window sections
    6. Verify LAYOUT-05: Scroll down — the footer (refresh/settings/quit buttons) scrolls with content, is NOT fixed at the bottom. A separator line appears above the footer.
    7. Verify LAYOUT-06: In the Statistics section, a thin horizontal line separates the "Models" row from the "Input" row
  </how-to-verify>
  <verify>
    <automated>dotnet build D:/myProjects/ccInfoWin/CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms all 6 LAYOUT requirements are visually correct in running app
  </acceptance_criteria>
  <done>All 6 LAYOUT requirements visually confirmed by human</done>
  <resume-signal>Type "approved" if all 6 layout changes are correct, or describe which ones need adjustment</resume-signal>
</task>

</tasks>

<verification>
1. `dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj` compiles without errors
2. `dotnet test CCInfoWindows.Tests/CCInfoWindows.Tests.csproj --arch x64` passes (no regressions)
3. Visual inspection confirms all 6 LAYOUT requirements in the running app
</verification>

<success_criteria>
- Footer is inside ScrollViewer, not a fixed Grid row (LAYOUT-05)
- Root Grid has RowDefinitions="Auto,*" (2 rows, not 3) (LAYOUT-05)
- Separator with Margin="0,4,0,0" exists above footer (LAYOUT-05)
- Separator with Margin="0,2,0,2" exists at Grid.Row="1" in Statistics grid (LAYOUT-06)
- Statistics grid has 9 RowDefinitions (LAYOUT-06)
- All existing tests pass, build succeeds
- Visual verification confirms correct layout
</success_criteria>

<output>
After completion, create `.planning/phases/09-layout-structure/09-02-SUMMARY.md`
</output>
