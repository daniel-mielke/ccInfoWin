---
phase: 11-behavior-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CCInfoWindows/CCInfoWindows/Helpers/CountdownFormatter.cs
  - CCInfoWindows.Tests/Helpers/CountdownFormatterTests.cs
  - CCInfoWindows/CCInfoWindows/Views/SettingsView.xaml
  - CCInfoWindows/CCInfoWindows/Views/MainView.xaml
autonomous: true
requirements: [TEXT-01, INTER-01, INTER-02]

must_haves:
  truths:
    - "Timer values >= 24 hours display as Xd Yh format (e.g. 3d 22h)"
    - "Logout button has red background, white text, and a logout icon left of the label"
    - "Login button has a login icon left of its label"
  artifacts:
    - path: "CCInfoWindows/CCInfoWindows/Helpers/CountdownFormatter.cs"
      provides: ">=24h branch in FormatCountdown"
      contains: "TotalDays"
    - path: "CCInfoWindows.Tests/Helpers/CountdownFormatterTests.cs"
      provides: "Tests for >=24h format"
      contains: "3d 22h"
    - path: "CCInfoWindows/CCInfoWindows/Views/SettingsView.xaml"
      provides: "Red logout button with icon"
      contains: "ProgressRedBrush"
    - path: "CCInfoWindows/CCInfoWindows/Views/MainView.xaml"
      provides: "Login button with icon"
      contains: "&#xE77B;"
  key_links:
    - from: "CCInfoWindows/CCInfoWindows/Helpers/CountdownFormatter.cs"
      to: "MainViewModel countdown properties"
      via: "FormatCountdown called from ViewModel"
      pattern: "FormatCountdown"
---

<objective>
Add >=24h timer format, style logout button red with icon, add login icon to re-login button.

Purpose: Correct timer display for long durations, improve button visual clarity with icons and color coding.
Output: Updated CountdownFormatter with tests, styled logout button in SettingsView, icon-decorated login button in MainView.
</objective>

<execution_context>
@C:/Users/DanielMielke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DanielMielke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-behavior-interaction/11-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add >=24h branch to CountdownFormatter with tests</name>
  <files>CCInfoWindows/CCInfoWindows/Helpers/CountdownFormatter.cs, CCInfoWindows.Tests/Helpers/CountdownFormatterTests.cs</files>
  <read_first>
    - CCInfoWindows/CCInfoWindows/Helpers/CountdownFormatter.cs
    - CCInfoWindows.Tests/Helpers/CountdownFormatterTests.cs
  </read_first>
  <behavior>
    - FormatCountdown(now + 3d 22h 15m) returns "3d 22h"
    - FormatCountdown(now + 1d 0h 30m) returns "1d 0h"
    - FormatCountdown(now + 24h exactly) returns "1d 0h"
    - FormatCountdown(now + 23h 59m) returns "23h 59min" (existing behavior unchanged)
    - FormatCountdown(now + 7d 0h 0m) returns "7d 0h"
  </behavior>
  <action>
RED phase: Add 5 test methods to CountdownFormatterTests.cs:
- `FormatCountdown_ThreeDays22Hours_ReturnsDaysHoursFormat`: future = now + 3d 22h 15m 30s, assert "3d 22h"
- `FormatCountdown_ExactlyOneDay_ReturnsDaysHoursFormat`: future = now + 24h 0m 30s, assert "1d 0h"
- `FormatCountdown_OneDayZeroMinutes_ReturnsDaysHoursFormat`: future = now + 1d 0h 30m 30s, assert "1d 0h"
- `FormatCountdown_SevenDays_ReturnsDaysHoursFormat`: future = now + 7d 0h 0m 30s, assert "7d 0h"
- `FormatCountdown_JustUnder24Hours_ReturnsHoursMinutes`: future = now + 23h 59m 30s, assert "23h 59min" (existing format)

Run tests — confirm new tests FAIL (existing ones still pass).

GREEN phase: In `FormatCountdown()`, insert a new branch BEFORE the existing `hours > 0` check:
```csharp
if (remaining.TotalHours >= 24)
{
    var days = (int)remaining.TotalDays;
    var hrs = remaining.Hours;
    return $"{days}d {hrs}h";
}
```
This goes after the `remaining <= TimeSpan.Zero` guard and BEFORE `var hours = (int)remaining.TotalHours;`.

Run tests — confirm ALL tests pass.

IMPORTANT: Use `remaining.Hours` (component property, 0-23) NOT `remaining.TotalHours` for the hours part. Use `(int)remaining.TotalDays` for days. Always English "d" — no localization.
  </action>
  <verify>
    <automated>dotnet test CCInfoWindows.Tests/CCInfoWindows.Tests.csproj --filter "FullyQualifiedName~CountdownFormatterTests" --no-restore</automated>
  </verify>
  <acceptance_criteria>
    - grep: "TotalDays" in CountdownFormatter.cs
    - grep: "remaining.Hours" in CountdownFormatter.cs (component hours, not TotalHours)
    - grep: 'd {hrs}h' OR 'd {' in CountdownFormatter.cs confirming format string
    - grep: "3d 22h" in CountdownFormatterTests.cs
    - grep: "1d 0h" in CountdownFormatterTests.cs
    - grep: "7d 0h" in CountdownFormatterTests.cs
    - All CountdownFormatterTests pass
  </acceptance_criteria>
  <done>FormatCountdown returns "Xd Yh" for durations >= 24 hours, with 5 new passing tests covering the boundary</done>
</task>

<task type="auto">
  <name>Task 2: Style logout button red with icon in SettingsView</name>
  <files>CCInfoWindows/CCInfoWindows/Views/SettingsView.xaml</files>
  <read_first>
    - CCInfoWindows/CCInfoWindows/Views/SettingsView.xaml
    - CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw
    - CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw
  </read_first>
  <action>
In SettingsView.xaml, replace the logout button (line ~147-152) from:
```xml
<Button l:Uids.Uid="SettingsLogoutButton"
        Grid.Row="2"
        Command="{x:Bind ViewModel.LogoutCommand}"
        HorizontalAlignment="Stretch"
        Margin="0,16,0,0"
        Style="{ThemeResource AccentButtonStyle}" />
```

To:
```xml
<Button Grid.Row="2"
        Command="{x:Bind ViewModel.LogoutCommand}"
        HorizontalAlignment="Stretch"
        Margin="0,16,0,0"
        Background="{ThemeResource ProgressRedBrush}"
        Foreground="White">
    <StackPanel Orientation="Horizontal" Spacing="8">
        <FontIcon Glyph="&#xE8FB;" FontSize="16" Foreground="White" />
        <TextBlock l:Uids.Uid="SettingsLogoutButton" Foreground="White" />
    </StackPanel>
</Button>
```

Key changes:
- REMOVE `Style="{ThemeResource AccentButtonStyle}"` (was blue)
- REMOVE `l:Uids.Uid` from Button (move to inner TextBlock)
- ADD `Background="{ThemeResource ProgressRedBrush}"` and `Foreground="White"`
- ADD explicit Content: StackPanel with FontIcon (Glyph E8FB) + TextBlock
- The TextBlock gets `l:Uids.Uid="SettingsLogoutButton"` so the `.Text` localization key resolves (existing key `SettingsLogoutButton.Content` won't work on TextBlock — need to check). Since TextBlock uses `.Text` property and the resw has `SettingsLogoutButton.Content`, we need to use a different approach: set the Uid on the TextBlock as a NEW key or hardcode with x:Uid targeting .Text.

CORRECTION: The existing resw key is `SettingsLogoutButton.Content`. TextBlock uses `.Text` not `.Content`. So either:
(a) Add new resw keys `SettingsLogoutButtonText.Text` = "Logout" / "Abmelden", OR
(b) Use the Uid `SettingsLogoutButton` on the TextBlock but the localizer resolves `.Text` for TextBlock, so add `SettingsLogoutButton.Text` keys to both resw files.

Approach (b): Add to en-US/Resources.resw:
```xml
<data name="SettingsLogoutButton.Text" xml:space="preserve">
    <value>Logout</value>
</data>
```
Add to de-DE/Resources.resw:
```xml
<data name="SettingsLogoutButton.Text" xml:space="preserve">
    <value>Abmelden</value>
</data>
```

Keep the existing `SettingsLogoutButton.Content` keys (they won't match anything now but are harmless; or remove them for cleanliness — prefer removal to avoid confusion).

Actually, REMOVE the old `.Content` keys and ADD `.Text` keys instead. The TextBlock in XAML gets `l:Uids.Uid="SettingsLogoutButton"` and WinUI3Localizer resolves `SettingsLogoutButton.Text` to set the Text property.
  </action>
  <verify>
    <automated>dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj</automated>
  </verify>
  <acceptance_criteria>
    - grep: "ProgressRedBrush" in SettingsView.xaml
    - grep: 'Foreground="White"' in SettingsView.xaml
    - grep: "&#xE8FB;" in SettingsView.xaml (logout icon glyph)
    - grep: 'Orientation="Horizontal" Spacing="8"' in SettingsView.xaml
    - grep: "AccentButtonStyle" NOT found in SettingsView.xaml
    - grep: "SettingsLogoutButton.Text" in en-US/Resources.resw AND de-DE/Resources.resw
    - Build succeeds
  </acceptance_criteria>
  <done>Logout button renders with red background, white text, and FontIcon E8FB left of localized label</done>
</task>

<task type="auto">
  <name>Task 3: Add login icon to ReLoginButton in MainView</name>
  <files>CCInfoWindows/CCInfoWindows/Views/MainView.xaml, CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw, CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw</files>
  <read_first>
    - CCInfoWindows/CCInfoWindows/Views/MainView.xaml (lines 56-67)
    - CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw
    - CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw
  </read_first>
  <action>
In MainView.xaml, replace the ReLoginButton (line ~65) from:
```xml
<Button l:Uids.Uid="ReLoginButton" Command="{x:Bind ViewModel.ReLoginCommand}" />
```

To:
```xml
<Button Command="{x:Bind ViewModel.ReLoginCommand}">
    <StackPanel Orientation="Horizontal" Spacing="8">
        <FontIcon Glyph="&#xE77B;" FontSize="16" />
        <TextBlock l:Uids.Uid="ReLoginButton" />
    </StackPanel>
</Button>
```

Key changes:
- REMOVE `l:Uids.Uid="ReLoginButton"` from Button element
- ADD explicit Content: StackPanel with FontIcon (Glyph E77B = sign-in icon) + TextBlock
- TextBlock gets `l:Uids.Uid="ReLoginButton"` — same localization approach as Task 2

For localization: The existing resw key is `ReLoginButton.Content`. TextBlock needs `.Text`. So:

Add to en-US/Resources.resw:
```xml
<data name="ReLoginButton.Text" xml:space="preserve">
    <value>Re-Login</value>
</data>
```
Add to de-DE/Resources.resw:
```xml
<data name="ReLoginButton.Text" xml:space="preserve">
    <value>Erneut anmelden</value>
</data>
```

Remove (or keep) the old `ReLoginButton.Content` keys. Prefer removal to avoid confusion.

NOTE: If Task 2 already modified the resw files, read them fresh before editing.
  </action>
  <verify>
    <automated>dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj</automated>
  </verify>
  <acceptance_criteria>
    - grep: "&#xE77B;" in MainView.xaml (login icon glyph)
    - grep: 'l:Uids.Uid="ReLoginButton"' in MainView.xaml (on TextBlock, not Button)
    - grep: "ReLoginButton.Text" in en-US/Resources.resw AND de-DE/Resources.resw
    - Build succeeds
  </acceptance_criteria>
  <done>Re-login button renders with FontIcon E77B (sign-in) left of localized label text</done>
</task>

</tasks>

<verification>
- `dotnet test CCInfoWindows.Tests/CCInfoWindows.Tests.csproj --filter "FullyQualifiedName~CountdownFormatterTests"` — all tests pass
- `dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj` — builds without errors
- grep confirms no `AccentButtonStyle` in SettingsView.xaml
- grep confirms `ProgressRedBrush`, `&#xE8FB;`, `&#xE77B;` present in respective XAML files
</verification>

<success_criteria>
1. CountdownFormatter.FormatCountdown returns "3d 22h" for 3 days 22 hours remaining
2. CountdownFormatter.FormatCountdown returns "23h 59min" for sub-24h (unchanged)
3. Logout button in SettingsView has red background (ProgressRedBrush), white text, E8FB icon
4. ReLogin button in MainView has E77B icon left of localized label
5. All existing and new tests pass
6. Project builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/11-behavior-interaction/11-01-SUMMARY.md`
</output>
