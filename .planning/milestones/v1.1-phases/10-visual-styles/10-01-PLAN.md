---
phase: 10-visual-styles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml
  - CCInfoWindows/CCInfoWindows/Views/MainView.xaml
autonomous: true
requirements:
  - STYLE-01
  - STYLE-02
  - STYLE-03
  - STYLE-04
  - TEXT-02
  - TEXT-03
  - TEXT-04

must_haves:
  truths:
    - "All progress bars render at 6px height including subagent bars"
    - "Progress bar tracks are gray semi-transparent (#72808080) in both themes"
    - "Project dropdown has rounded corners and matches Segmented tab background"
    - "Model badges display as fully rounded pills"
    - "Statistics Total and Cost labels use secondary text color with normal weight"
    - "Total row has 8px top margin for spacing"
  artifacts:
    - path: "CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml"
      provides: "Updated ProgressTrackBrush color"
      contains: "#72808080"
    - path: "CCInfoWindows/CCInfoWindows/Views/MainView.xaml"
      provides: "Updated XAML attributes for bars, badges, ComboBox, statistics labels"
  key_links:
    - from: "MainView.xaml ProgressBars"
      to: "AppTheme.xaml ProgressTrackBrush"
      via: "ThemeResource binding"
      pattern: "ProgressTrackBrush"
    - from: "MainView.xaml ComboBox"
      to: "AppTheme.xaml SegmentedBackgroundBrush"
      via: "ThemeResource binding"
      pattern: "SegmentedBackgroundBrush"
---

<objective>
Apply uniform visual styling to progress bars, ComboBox, model badges, and statistics labels in MainView.xaml, and update the ProgressTrackBrush color in AppTheme.xaml.

Purpose: Achieve visual consistency across all UI elements per the v1.7.1 reference design.
Output: Updated AppTheme.xaml and MainView.xaml with all XAML-only style changes applied.
</objective>

<execution_context>
@C:/Users/DanielMielke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DanielMielke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-visual-styles/10-CONTEXT.md

<interfaces>
From CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml:
- Dark theme ProgressTrackBrush currently: Color="#38383A" (line 12)
- Light theme ProgressTrackBrush currently: Color="#D1D1D6" (line 33)
- SegmentedBackgroundBrush exists in both themes (Dark: #38383A, Light: #E5E5EA)
- SecondaryTextBrush exists in both themes (Dark: #8E8E93, Light: #6E6E73)

From CCInfoWindows/CCInfoWindows/Views/MainView.xaml (key locations):
- Line ~91-107: ComboBox element — needs Background and CornerRadius attributes
- Line ~153: Main context badge Border CornerRadius="6" — needs "999"
- Line ~196: Subagent badge Border CornerRadius="6" — needs "999"
- Line ~206-210: Subagent ProgressBar Height="4" — needs "6"
- Line ~551-552: StatsTotal TextBlock — needs Foreground + Margin changes
- Line ~563-564: StatsCost TextBlock — needs Foreground + FontWeight changes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AppTheme.xaml ProgressTrackBrush and MainView.xaml XAML attributes</name>
  <files>CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml, CCInfoWindows/CCInfoWindows/Views/MainView.xaml</files>
  <read_first>
    - CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml (current ProgressTrackBrush values)
    - CCInfoWindows/CCInfoWindows/Views/MainView.xaml (current attribute values for all targets)
  </read_first>
  <action>
**AppTheme.xaml changes (STYLE-02):**
1. In the Dark theme section (x:Key="Dark"), change the ProgressTrackBrush Color from `#38383A` to `#72808080`
2. In the Light theme section (x:Key="Light"), change the ProgressTrackBrush Color from `#D1D1D6` to `#72808080`

**MainView.xaml changes:**

STYLE-01 — Subagent ProgressBar height:
3. Find the subagent ProgressBar (near line ~206-210, inside the subagent ItemsRepeater DataTemplate). Change `Height="4"` to `Height="6"`

STYLE-03 — ComboBox styling:
4. Find the ComboBox element (near line ~91-107). Add two attributes:
   - `Background="{ThemeResource SegmentedBackgroundBrush}"`
   - `CornerRadius="8"`

STYLE-04 — Model badge pill corners:
5. Find the main context badge Border element (near line ~153) with `CornerRadius="6"`. Change to `CornerRadius="999"`
6. Find the subagent badge Border element (near line ~196) with `CornerRadius="6"`. Change to `CornerRadius="999"`

TEXT-02 — Statistics label foreground:
7. Find the StatsTotal TextBlock (x:Name="StatsTotal" or near line ~551). Change its `Foreground` from `{ThemeResource PrimaryTextBrush}` to `{ThemeResource SecondaryTextBrush}`
8. Find the StatsCost TextBlock (x:Name="StatsCost" or near line ~563). Change its `Foreground` from `{ThemeResource PrimaryTextBrush}` to `{ThemeResource SecondaryTextBrush}`

TEXT-03 — Statistics cost font weight:
9. On the StatsCost TextBlock, change `FontWeight="SemiBold"` to `FontWeight="Normal"`

TEXT-04 — Statistics total spacing:
10. On the StatsTotal TextBlock, add `Margin="0,8,0,0"` (or update existing Margin to include top=8)
  </action>
  <verify>
    <automated>dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj</automated>
  </verify>
  <acceptance_criteria>
    - AppTheme.xaml Dark section contains: `Color="#72808080"` on the ProgressTrackBrush line
    - AppTheme.xaml Light section contains: `Color="#72808080"` on the ProgressTrackBrush line
    - MainView.xaml subagent ProgressBar contains `Height="6"` (no Height="4" on any ProgressBar)
    - MainView.xaml ComboBox contains `Background="{ThemeResource SegmentedBackgroundBrush}"` and `CornerRadius="8"`
    - MainView.xaml main context badge Border contains `CornerRadius="999"`
    - MainView.xaml subagent badge Border contains `CornerRadius="999"`
    - MainView.xaml StatsTotal TextBlock contains `Foreground="{ThemeResource SecondaryTextBrush}"` and `Margin="0,8,0,0"`
    - MainView.xaml StatsCost TextBlock contains `Foreground="{ThemeResource SecondaryTextBrush}"` and `FontWeight="Normal"`
    - `dotnet build` succeeds with no errors
  </acceptance_criteria>
  <done>All 7 XAML-only requirements (STYLE-01, STYLE-02, STYLE-03, STYLE-04, TEXT-02, TEXT-03, TEXT-04) applied. Build passes.</done>
</task>

</tasks>

<verification>
1. `dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj` compiles without errors
2. grep confirms no remaining `Height="4"` on any ProgressBar element
3. grep confirms no remaining `CornerRadius="6"` on any model badge Border
4. grep confirms `#72808080` appears twice in AppTheme.xaml (Dark + Light ProgressTrackBrush)
</verification>

<success_criteria>
- All ProgressBars have Height="6"
- ProgressTrackBrush is #72808080 in both themes
- ComboBox has SegmentedBackgroundBrush background and CornerRadius="8"
- Model badges have CornerRadius="999"
- StatsTotal and StatsCost use SecondaryTextBrush foreground
- StatsCost has FontWeight="Normal"
- StatsTotal has Margin="0,8,0,0"
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/10-visual-styles/10-01-SUMMARY.md`
</output>
