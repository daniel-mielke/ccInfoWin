---
phase: 05-cost-analytics
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj
  - CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml
  - CCInfoWindows/CCInfoWindows/ViewModels/MainViewModel.cs
  - CCInfoWindows/CCInfoWindows/Views/MainView.xaml
  - CCInfoWindows/CCInfoWindows/Views/MainView.xaml.cs
  - CCInfoWindows/CCInfoWindows/ViewModels/SettingsViewModel.cs
  - CCInfoWindows/CCInfoWindows/Views/SettingsView.xaml
  - CCInfoWindows/CCInfoWindows/App.xaml.cs
  - CCInfoWindows.Tests/ViewModels/MainViewModelStatisticsTests.cs
autonomous: false
requirements:
  - TOKS-02
  - COST-05
  - COST-06

must_haves:
  truths:
    - "User sees a 4-segment tab bar (Session/Heute/Woche/Monat) that switches token/cost display"
    - "Session tab shows data instantly; Heute/Woche/Monat tabs show shimmer during aggregation"
    - "Statistics table shows Modelle, Eingabe, Ausgabe, Cache-Schreiben, Cache-Lesen, Gesamt, Kosten, Burn Rate"
    - "Estimated costs display with tilde prefix (~$X.XX)"
    - "Burn Rate displayed below cost row, computed from BurnRateEntries via BurnRateCalculator"
    - "Settings page shows pricing data source (Live/Fallback) and last fetch time"
  artifacts:
    - path: "CCInfoWindows/CCInfoWindows/Views/MainView.xaml"
      provides: "STATISTIKEN section with Segmented control and data table"
      contains: "controls:Segmented"
    - path: "CCInfoWindows/CCInfoWindows/ViewModels/MainViewModel.cs"
      provides: "SelectedTabIndex, IsAggregating, statistics properties, ApplyStatistics with BurnRateCalculator call"
      contains: "BurnRateCalculator.ComputeBurnRate"
    - path: "CCInfoWindows/CCInfoWindows/Views/SettingsView.xaml"
      provides: "Pricing data source and last fetch display"
      contains: "Preisdaten"
    - path: "CCInfoWindows.Tests/ViewModels/MainViewModelStatisticsTests.cs"
      provides: "Tests for SelectedTabIndex triggering IsAggregating and statistics update"
      contains: "MainViewModelStatisticsTests"
  key_links:
    - from: "CCInfoWindows/CCInfoWindows/ViewModels/MainViewModel.cs"
      to: "CCInfoWindows/CCInfoWindows/Services/Interfaces/IJsonlService.cs"
      via: "GetStatistics(timePeriod) call on tab switch"
      pattern: "GetStatistics"
    - from: "CCInfoWindows/CCInfoWindows/ViewModels/MainViewModel.cs"
      to: "CCInfoWindows/CCInfoWindows/Helpers/BurnRateCalculator.cs"
      via: "ApplyStatistics calls ComputeBurnRate with stats.BurnRateEntries"
      pattern: "BurnRateCalculator\\.ComputeBurnRate"
    - from: "CCInfoWindows/CCInfoWindows/Views/MainView.xaml"
      to: "CCInfoWindows/CCInfoWindows/ViewModels/MainViewModel.cs"
      via: "x:Bind for SelectedTabIndex, IsAggregating, statistics text"
      pattern: "x:Bind ViewModel\\."
    - from: "CCInfoWindows/CCInfoWindows/App.xaml.cs"
      to: "CCInfoWindows/CCInfoWindows/Services/LiteLLMPricingService.cs"
      via: "DI registration as IPricingService singleton"
      pattern: "AddSingleton.*IPricingService"
---

<objective>
Wire the complete STATISTIKEN UI: install Segmented NuGet package, add theme resources, replace the TOKENS section with tab bar + statistics table + shimmer, add ViewModel properties for tab switching and aggregation, add settings pricing info display, register all new services in DI. Includes burn rate calculation wiring and unit tests for tab-switch trigger logic.

Purpose: This is the user-facing surface for cost analytics. Without this plan, all the backend logic from Plan 01 has no UI.

Output: 8 modified files -- NuGet package added, theme resources extended, MainView STATISTIKEN section, MainViewModel tab/statistics state, SettingsView pricing info, App.xaml.cs DI wiring. 1 new test file for ViewModel statistics behavior.
</objective>

<execution_context>
@C:/Users/DanielMielke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DanielMielke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cost-analytics/05-CONTEXT.md
@.planning/phases/05-cost-analytics/05-RESEARCH.md
@.planning/phases/05-cost-analytics/05-UI-SPEC.md
@.planning/phases/05-cost-analytics/05-01-SUMMARY.md

<interfaces>
<!-- Contracts created by Plan 01 that this plan consumes. -->

From CCInfoWindows/CCInfoWindows/Models/TimePeriod.cs:
```csharp
public enum TimePeriod { Session, Today, Week, Month }
```

From CCInfoWindows/CCInfoWindows/Models/StatisticsSummary.cs:
```csharp
public record StatisticsSummary
{
    public static readonly StatisticsSummary Empty = new();
    public long InputTokens { get; init; }
    public long OutputTokens { get; init; }
    public long CacheCreationTokens { get; init; }
    public long CacheReadTokens { get; init; }
    public long TotalTokens => InputTokens + OutputTokens + CacheCreationTokens + CacheReadTokens;
    public decimal TotalCostUsd { get; init; }
    public bool HasEstimatedCosts { get; init; }
    public IReadOnlyList<string> Models { get; init; } = [];
    public IReadOnlyList<(DateTimeOffset Timestamp, long Tokens)> BurnRateEntries { get; init; } = [];
}
```

From CCInfoWindows/CCInfoWindows/Services/Interfaces/IPricingService.cs:
```csharp
public enum PricingSource { Live, Fallback, Unknown }
public interface IPricingService
{
    ModelPricing? GetPrice(string modelName);
    PricingSource Source { get; }
    DateTimeOffset? LastFetch { get; }
    Task EnsurePricesLoadedAsync();
}
```

From CCInfoWindows/CCInfoWindows/Services/Interfaces/IJsonlService.cs (extended):
```csharp
StatisticsSummary GetStatistics(TimePeriod period, string? sessionId = null);
```

From CCInfoWindows/CCInfoWindows/Helpers/CostFormatter.cs:
```csharp
public static string FormatCost(decimal amount, bool isEstimated);
// Returns "$4.23" or "~$4.23"
```

From CCInfoWindows/CCInfoWindows/Helpers/BurnRateCalculator.cs:
```csharp
public static double ComputeBurnRate(IEnumerable<(DateTimeOffset Timestamp, long Tokens)> entries, int windowMinutes = 60);
// Returns tokens/hour
```

From CCInfoWindows/CCInfoWindows/Helpers/TokenFormatter.cs:
```csharp
public static string FormatTokenCount(long tokens); // "123", "1.2K", "1.2M"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NuGet package, theme resources, DI wiring, ViewModel properties with burn rate wiring, and tests</name>
  <files>
    CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj,
    CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml,
    CCInfoWindows/CCInfoWindows/App.xaml.cs,
    CCInfoWindows/CCInfoWindows/ViewModels/MainViewModel.cs,
    CCInfoWindows/CCInfoWindows/ViewModels/SettingsViewModel.cs,
    CCInfoWindows.Tests/ViewModels/MainViewModelStatisticsTests.cs
  </files>
  <read_first>
    CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj,
    CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml,
    CCInfoWindows/CCInfoWindows/App.xaml.cs,
    CCInfoWindows/CCInfoWindows/ViewModels/MainViewModel.cs,
    CCInfoWindows/CCInfoWindows/ViewModels/SettingsViewModel.cs,
    CCInfoWindows/CCInfoWindows/Services/Interfaces/IPricingService.cs,
    CCInfoWindows/CCInfoWindows/Services/Interfaces/IJsonlService.cs,
    CCInfoWindows/CCInfoWindows/Models/StatisticsSummary.cs,
    CCInfoWindows/CCInfoWindows/Models/TimePeriod.cs,
    CCInfoWindows/CCInfoWindows/Helpers/CostFormatter.cs,
    CCInfoWindows/CCInfoWindows/Helpers/BurnRateCalculator.cs,
    CCInfoWindows/CCInfoWindows/Helpers/TokenFormatter.cs,
    .planning/phases/05-cost-analytics/05-UI-SPEC.md,
    .planning/phases/05-cost-analytics/05-CONTEXT.md
  </read_first>
  <behavior>
    - Setting SelectedTabIndex to 0 (Session) calls UpdateStatisticsFromSession, IsAggregating stays false
    - Setting SelectedTabIndex to 1/2/3 (Heute/Woche/Monat) sets IsAggregating=true, then calls GetStatistics with correct TimePeriod, then sets IsAggregating=false
    - ApplyStatistics with non-empty BurnRateEntries calls BurnRateCalculator.ComputeBurnRate and formats result as "X.XK T/h"
    - ApplyStatistics with empty BurnRateEntries sets StatisticsBurnRate to "0 T/h"
    - ApplyStatistics with HasEstimatedCosts=true produces cost string with tilde prefix
  </behavior>
  <action>
    **0. Create `MainViewModelStatisticsTests.cs`** in CCInfoWindows.Tests/ViewModels/ (TDD red phase):
    Write tests FIRST before modifying MainViewModel. Create a test class that mocks `IJsonlService` and `IPricingService` to test the tab-switch/statistics behavior:
    ```csharp
    public class MainViewModelStatisticsTests
    {
        // Test 1: SelectedTabIndex=0 does NOT set IsAggregating
        // Mock IJsonlService.GetStatistics(Session, sessionId) to return a StatisticsSummary
        // Assert IsAggregating remains false after tab switch

        // Test 2: SelectedTabIndex=1 sets IsAggregating=true initially
        // Mock IJsonlService.GetStatistics(Today) to return a StatisticsSummary
        // Assert IsAggregating is true during aggregation (use TaskCompletionSource to control timing)

        // Test 3: ApplyStatistics with BurnRateEntries computes correct burn rate
        // Create a StatisticsSummary with BurnRateEntries containing entries from last 60 min
        // Assert StatisticsBurnRate is NOT "0 T/h"

        // Test 4: ApplyStatistics with empty BurnRateEntries sets "0 T/h"
        // Create StatisticsSummary.Empty
        // Assert StatisticsBurnRate == "0 T/h"

        // Test 5: ApplyStatistics with HasEstimatedCosts=true produces tilde cost
        // Create StatisticsSummary with HasEstimatedCosts=true, TotalCostUsd=5.0m
        // Assert StatisticsCost starts with "~"
    }
    ```
    Use Moq for IJsonlService and IPricingService mocks. The MainViewModel constructor needs to be testable — if it requires DispatcherQueue, test only the `ApplyStatistics` method directly (make it `internal` with `[InternalsVisibleTo]` if needed, or test via public property changes).

    **1. Install NuGet package:**
    Run: `dotnet add CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj package CommunityToolkit.WinUI.Controls.Segmented --version 8.2.251219`

    **2. Add theme resources to `AppTheme.xaml`:**
    Add these 4 new brush resources inside the existing Dark theme ResourceDictionary:
    ```xml
    <SolidColorBrush x:Key="SegmentedBackgroundBrush" Color="#38383A" />
    <SolidColorBrush x:Key="SegmentedActiveBackgroundBrush" Color="#636366" />
    <SolidColorBrush x:Key="ShimmerBaseBrush" Color="#38383A" />
    <SolidColorBrush x:Key="ShimmerHighlightBrush" Color="#555558" />
    ```
    And for the Light theme ResourceDictionary:
    ```xml
    <SolidColorBrush x:Key="SegmentedBackgroundBrush" Color="#E5E5EA" />
    <SolidColorBrush x:Key="SegmentedActiveBackgroundBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="ShimmerBaseBrush" Color="#E5E5EA" />
    <SolidColorBrush x:Key="ShimmerHighlightBrush" Color="#D1D1D6" />
    ```

    **3. Register services in `App.xaml.cs`:**
    Add to the DI container registration section (after the existing `IJsonlService` registration):
    ```csharp
    services.AddSingleton<IPricingService>(sp =>
        new LiteLLMPricingService(sp.GetRequiredService<HttpClient>()));
    ```
    Add `using CCInfoWindows.Services.Interfaces;` and `using CCInfoWindows.Services;` if not already present.

    **Important:** The `JsonlService` constructor now requires `IPricingService`. Update the `IJsonlService` registration to:
    ```csharp
    services.AddSingleton<IJsonlService>(sp =>
        new JsonlService(pricingService: sp.GetRequiredService<IPricingService>()));
    ```
    If the existing registration is `services.AddSingleton<IJsonlService, JsonlService>()`, change it to the factory form above so the pricing service is injected.

    **4. Extend `MainViewModel.cs`:**

    **4a. Add new constructor parameter:** `IPricingService pricingService`. Store as `private readonly IPricingService _pricingService;`.

    **4b. Replace the token counter properties** (`_inputTokensText`, `_outputTokensText`) with the full statistics set:
    ```csharp
    // --- Statistics (STATISTIKEN section) ---

    [ObservableProperty]
    private int _selectedTabIndex;

    [ObservableProperty]
    private bool _isAggregating;

    [ObservableProperty]
    private string _statisticsModels = "\u2013";

    [ObservableProperty]
    private string _statisticsInput = "\u2013";

    [ObservableProperty]
    private string _statisticsOutput = "\u2013";

    [ObservableProperty]
    private string _statisticsCacheCreation = "\u2013";

    [ObservableProperty]
    private string _statisticsCacheRead = "\u2013";

    [ObservableProperty]
    private string _statisticsTotal = "\u2013";

    [ObservableProperty]
    private string _statisticsCost = "\u2013";

    [ObservableProperty]
    private string _statisticsBurnRate = "0 T/h";
    ```
    Keep `_inputTokensText` and `_outputTokensText` temporarily for backward compat if any other code references them, OR remove them if only used in MainView TOKENS section (which we replace).

    **4c. Add `OnSelectedTabIndexChanged` partial method:**
    ```csharp
    partial void OnSelectedTabIndexChanged(int value)
    {
        var period = (TimePeriod)value;
        if (period == TimePeriod.Session)
        {
            // Session tab: instant from cache
            UpdateStatisticsFromSession();
        }
        else
        {
            // Other tabs: background aggregation with shimmer
            _ = AggregateStatisticsAsync(period);
        }
    }
    ```

    **4d. Add `UpdateStatisticsFromSession()` private method:**
    ```csharp
    private void UpdateStatisticsFromSession()
    {
        if (SelectedSession == null)
        {
            ApplyStatistics(StatisticsSummary.Empty);
            return;
        }
        var stats = _jsonlService.GetStatistics(TimePeriod.Session, SelectedSession.Session.Id);
        ApplyStatistics(stats);
    }
    ```

    **4e. Add `AggregateStatisticsAsync()` private method:**
    ```csharp
    private async Task AggregateStatisticsAsync(TimePeriod period)
    {
        IsAggregating = true;
        try
        {
            await _pricingService.EnsurePricesLoadedAsync();
            var stats = await Task.Run(() => _jsonlService.GetStatistics(period));
            DispatcherQueue.GetForCurrentThread().TryEnqueue(() => ApplyStatistics(stats));
        }
        catch
        {
            DispatcherQueue.GetForCurrentThread().TryEnqueue(() => ApplyStatistics(StatisticsSummary.Empty));
        }
        finally
        {
            DispatcherQueue.GetForCurrentThread().TryEnqueue(() => IsAggregating = false);
        }
    }
    ```

    **Note on DispatcherQueue usage:** Since AggregateStatisticsAsync may be called from UI thread via property change, the `await Task.Run` pushes the heavy aggregation to the thread pool. The results are dispatched back to the UI thread. Use the DispatcherQueue pattern established in the project.

    **4f. Add `ApplyStatistics()` method** (internal for testability):
    ```csharp
    internal void ApplyStatistics(StatisticsSummary stats)
    {
        StatisticsModels = stats.Models.Count > 0
            ? string.Join(", ", stats.Models.Select(m => ModelContextLimits.GetDisplayName(m)))
            : "\u2013";
        StatisticsInput = stats.InputTokens > 0 ? TokenFormatter.FormatTokenCount(stats.InputTokens) : "\u2013";
        StatisticsOutput = stats.OutputTokens > 0 ? TokenFormatter.FormatTokenCount(stats.OutputTokens) : "\u2013";
        StatisticsCacheCreation = stats.CacheCreationTokens > 0 ? TokenFormatter.FormatTokenCount(stats.CacheCreationTokens) : "\u2013";
        StatisticsCacheRead = stats.CacheReadTokens > 0 ? TokenFormatter.FormatTokenCount(stats.CacheReadTokens) : "\u2013";
        StatisticsTotal = stats.TotalTokens > 0 ? TokenFormatter.FormatTokenCount(stats.TotalTokens) : "\u2013";
        StatisticsCost = CostFormatter.FormatCost(stats.TotalCostUsd, stats.HasEstimatedCosts);

        // Burn rate: compute from BurnRateEntries using BurnRateCalculator (COST-05)
        var burnRate = BurnRateCalculator.ComputeBurnRate(stats.BurnRateEntries);
        StatisticsBurnRate = burnRate > 0
            ? $"{TokenFormatter.FormatTokenCount((long)burnRate)} T/h"
            : "0 T/h";
    }
    ```

    **Critical change vs. previous plan:** `ApplyStatistics` now calls `BurnRateCalculator.ComputeBurnRate(stats.BurnRateEntries)` with the actual entry data from `StatisticsSummary.BurnRateEntries`, instead of using a static placeholder. The burn rate entries are populated by `GetStatistics()` in Plan 01's JsonlService implementation.

    **4g. Update `UpdateSessionData()`:** After existing token update, also refresh statistics if on Session tab:
    ```csharp
    // Replace the old InputTokensText / OutputTokensText lines with:
    if (SelectedTabIndex == 0) // Session tab
        UpdateStatisticsFromSession();
    ```

    **4h. Update `ClearSessionData()`:** Reset all statistics properties to em-dash defaults, set `SelectedTabIndex = 0`.

    **4i. In `InitializeAsync()`:** After `_jsonlService.InitializeAsync()`, call `await _pricingService.EnsurePricesLoadedAsync()` (non-blocking -- pricing loads in background).

    **4j. Add `[InternalsVisibleTo]`:** If not already present, add `[assembly: InternalsVisibleTo("CCInfoWindows.Tests")]` to make `ApplyStatistics` testable. If the project already uses this pattern, skip.

    **5. Extend `SettingsViewModel.cs`:**
    Add `IPricingService` as constructor parameter. Add two read-only properties:
    ```csharp
    private readonly IPricingService _pricingService;

    public string PricingSourceText => _pricingService.Source switch
    {
        PricingSource.Live => "Live (LiteLLM API)",
        PricingSource.Fallback => "Fallback (geb\u00fcndelt)",
        _ => "Unbekannt"
    };

    public string LastPricingFetchText => _pricingService.LastFetch.HasValue
        ? _pricingService.LastFetch.Value.LocalDateTime.ToString("dd.MM.yyyy HH:mm")
        : "Nie";
    ```
    Initialize these in the constructor or via `OnNavigatedTo` pattern.
  </action>
  <verify>
    <automated>dotnet test CCInfoWindows.Tests/CCInfoWindows.Tests.csproj -p:Platform=x64 --filter "FullyQualifiedName~MainViewModelStatisticsTests"</automated>
  </verify>
  <acceptance_criteria>
    - CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj contains `CommunityToolkit.WinUI.Controls.Segmented`
    - CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml contains `SegmentedBackgroundBrush` and `ShimmerBaseBrush`
    - CCInfoWindows/CCInfoWindows/App.xaml.cs contains `AddSingleton` for `IPricingService`
    - CCInfoWindows/CCInfoWindows/ViewModels/MainViewModel.cs contains `_selectedTabIndex` field with [ObservableProperty]
    - CCInfoWindows/CCInfoWindows/ViewModels/MainViewModel.cs contains `_isAggregating` field with [ObservableProperty]
    - CCInfoWindows/CCInfoWindows/ViewModels/MainViewModel.cs contains `OnSelectedTabIndexChanged` method
    - CCInfoWindows/CCInfoWindows/ViewModels/MainViewModel.cs contains `AggregateStatisticsAsync` method
    - CCInfoWindows/CCInfoWindows/ViewModels/MainViewModel.cs contains `ApplyStatistics` method with `BurnRateCalculator.ComputeBurnRate(stats.BurnRateEntries)` call
    - CCInfoWindows/CCInfoWindows/ViewModels/MainViewModel.cs does NOT contain `StatisticsBurnRate = "0 T/h"; // Will be set properly` placeholder comment
    - CCInfoWindows/CCInfoWindows/ViewModels/MainViewModel.cs contains `_statisticsCost` and `_statisticsBurnRate`
    - CCInfoWindows/CCInfoWindows/ViewModels/SettingsViewModel.cs contains `PricingSourceText` property
    - CCInfoWindows/CCInfoWindows/ViewModels/SettingsViewModel.cs contains `LastPricingFetchText` property
    - CCInfoWindows.Tests/ViewModels/MainViewModelStatisticsTests.cs has at least 5 [Fact] methods
    - `dotnet test` for MainViewModelStatisticsTests exits 0
    - `dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj` exits 0
  </acceptance_criteria>
  <done>NuGet package installed, theme brushes added, DI wiring complete, MainViewModel has tab switching with shimmer state and statistics properties including burn rate computed via BurnRateCalculator, SettingsViewModel exposes pricing info. Tests verify tab-switch trigger logic and burn rate wiring. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: STATISTIKEN XAML section with tab bar, data table, shimmer, and settings pricing rows</name>
  <files>
    CCInfoWindows/CCInfoWindows/Views/MainView.xaml,
    CCInfoWindows/CCInfoWindows/Views/MainView.xaml.cs,
    CCInfoWindows/CCInfoWindows/Views/SettingsView.xaml
  </files>
  <read_first>
    CCInfoWindows/CCInfoWindows/Views/MainView.xaml,
    CCInfoWindows/CCInfoWindows/Views/MainView.xaml.cs,
    CCInfoWindows/CCInfoWindows/Views/SettingsView.xaml,
    CCInfoWindows/CCInfoWindows/ViewModels/MainViewModel.cs,
    CCInfoWindows/CCInfoWindows/ViewModels/SettingsViewModel.cs,
    CCInfoWindows/CCInfoWindows/Resources/AppTheme.xaml,
    .planning/phases/05-cost-analytics/05-UI-SPEC.md,
    .planning/phases/05-cost-analytics/05-CONTEXT.md
  </read_first>
  <action>
    **1. Update `MainView.xaml`:**

    **1a. Add namespace for CommunityToolkit Segmented control** at the top of the Page element:
    ```xml
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    ```

    **1b. Replace the entire TOKENS section** (the `<!-- Divider before TOKENS -->` border through the closing `</StackPanel>` of the TOKENS section) with:

    ```xml
    <!-- Divider before STATISTIKEN -->
    <Border Height="1" Background="{ThemeResource DividerBrush}" />

    <!-- ==================== STATISTIKEN ==================== -->
    <StackPanel Spacing="8">
        <TextBlock Text="STATISTIKEN"
                   FontSize="11" FontWeight="SemiBold"
                   Foreground="{ThemeResource SectionHeaderBrush}"
                   CharacterSpacing="50" />

        <!-- Tab Bar -->
        <controls:Segmented
            x:Name="StatisticsTabBar"
            HorizontalAlignment="Stretch"
            SelectedIndex="{x:Bind ViewModel.SelectedTabIndex, Mode=TwoWay}"
            SelectionMode="Single">
            <controls:Segmented.Resources>
                <!-- Override default toolkit brushes to match styleguide -->
                <SolidColorBrush x:Key="SegmentedControlBackground" Color="{ThemeResource SegmentedBackgroundBrush}" />
            </controls:Segmented.Resources>
            <controls:SegmentedItem Content="Session" />
            <controls:SegmentedItem Content="Heute" />
            <controls:SegmentedItem Content="Woche" />
            <controls:SegmentedItem Content="Monat" />
        </controls:Segmented>

        <!-- Statistics Data Table -->
        <Grid RowSpacing="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- Row 0: Modelle -->
                <RowDefinition Height="Auto" /> <!-- Row 1: Eingabe -->
                <RowDefinition Height="Auto" /> <!-- Row 2: Ausgabe -->
                <RowDefinition Height="Auto" /> <!-- Row 3: Cache-Schreiben -->
                <RowDefinition Height="Auto" /> <!-- Row 4: Cache-Lesen -->
                <RowDefinition Height="Auto" /> <!-- Row 5: Divider -->
                <RowDefinition Height="Auto" /> <!-- Row 6: Gesamt -->
                <RowDefinition Height="Auto" /> <!-- Row 7: Kosten -->
                <RowDefinition Height="Auto" /> <!-- Row 8: Burn Rate -->
            </Grid.RowDefinitions>

            <!-- Row 0: Modelle -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Modelle:" FontSize="13"
                       Foreground="{ThemeResource SecondaryTextBrush}" />
            <TextBlock Grid.Row="0" Grid.Column="1"
                       Text="{x:Bind ViewModel.StatisticsModels, Mode=OneWay}"
                       FontSize="13" HorizontalAlignment="Right"
                       Foreground="{ThemeResource PrimaryTextBrush}"
                       TextTrimming="CharacterEllipsis" MaxLines="1"
                       Visibility="{x:Bind local:MainView.InvertBool(ViewModel.IsAggregating), Mode=OneWay}" />
            <Border Grid.Row="0" Grid.Column="1" Height="13" Width="60" CornerRadius="4"
                    HorizontalAlignment="Right" Background="{ThemeResource ShimmerBaseBrush}"
                    Visibility="{x:Bind ViewModel.IsAggregating, Mode=OneWay,
                                Converter={StaticResource BoolToVisibilityConverter}}" />

            <!-- Row 1: Eingabe -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Eingabe:" FontSize="13"
                       Foreground="{ThemeResource SecondaryTextBrush}" />
            <TextBlock Grid.Row="1" Grid.Column="1"
                       Text="{x:Bind ViewModel.StatisticsInput, Mode=OneWay}"
                       FontSize="13" FontWeight="Medium" HorizontalAlignment="Right"
                       Foreground="{ThemeResource PrimaryTextBrush}"
                       Visibility="{x:Bind local:MainView.InvertBool(ViewModel.IsAggregating), Mode=OneWay}" />
            <Border Grid.Row="1" Grid.Column="1" Height="13" Width="60" CornerRadius="4"
                    HorizontalAlignment="Right" Background="{ThemeResource ShimmerBaseBrush}"
                    Visibility="{x:Bind ViewModel.IsAggregating, Mode=OneWay,
                                Converter={StaticResource BoolToVisibilityConverter}}" />

            <!-- Row 2: Ausgabe -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Ausgabe:" FontSize="13"
                       Foreground="{ThemeResource SecondaryTextBrush}" />
            <TextBlock Grid.Row="2" Grid.Column="1"
                       Text="{x:Bind ViewModel.StatisticsOutput, Mode=OneWay}"
                       FontSize="13" FontWeight="Medium" HorizontalAlignment="Right"
                       Foreground="{ThemeResource PrimaryTextBrush}"
                       Visibility="{x:Bind local:MainView.InvertBool(ViewModel.IsAggregating), Mode=OneWay}" />
            <Border Grid.Row="2" Grid.Column="1" Height="13" Width="60" CornerRadius="4"
                    HorizontalAlignment="Right" Background="{ThemeResource ShimmerBaseBrush}"
                    Visibility="{x:Bind ViewModel.IsAggregating, Mode=OneWay,
                                Converter={StaticResource BoolToVisibilityConverter}}" />

            <!-- Row 3: Cache-Schreiben -->
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Cache-Schreiben:" FontSize="13"
                       Foreground="{ThemeResource SecondaryTextBrush}" />
            <TextBlock Grid.Row="3" Grid.Column="1"
                       Text="{x:Bind ViewModel.StatisticsCacheCreation, Mode=OneWay}"
                       FontSize="13" FontWeight="Medium" HorizontalAlignment="Right"
                       Foreground="{ThemeResource PrimaryTextBrush}"
                       Visibility="{x:Bind local:MainView.InvertBool(ViewModel.IsAggregating), Mode=OneWay}" />
            <Border Grid.Row="3" Grid.Column="1" Height="13" Width="60" CornerRadius="4"
                    HorizontalAlignment="Right" Background="{ThemeResource ShimmerBaseBrush}"
                    Visibility="{x:Bind ViewModel.IsAggregating, Mode=OneWay,
                                Converter={StaticResource BoolToVisibilityConverter}}" />

            <!-- Row 4: Cache-Lesen -->
            <TextBlock Grid.Row="4" Grid.Column="0" Text="Cache-Lesen:" FontSize="13"
                       Foreground="{ThemeResource SecondaryTextBrush}" />
            <TextBlock Grid.Row="4" Grid.Column="1"
                       Text="{x:Bind ViewModel.StatisticsCacheRead, Mode=OneWay}"
                       FontSize="13" FontWeight="Medium" HorizontalAlignment="Right"
                       Foreground="{ThemeResource PrimaryTextBrush}"
                       Visibility="{x:Bind local:MainView.InvertBool(ViewModel.IsAggregating), Mode=OneWay}" />
            <Border Grid.Row="4" Grid.Column="1" Height="13" Width="60" CornerRadius="4"
                    HorizontalAlignment="Right" Background="{ThemeResource ShimmerBaseBrush}"
                    Visibility="{x:Bind ViewModel.IsAggregating, Mode=OneWay,
                                Converter={StaticResource BoolToVisibilityConverter}}" />

            <!-- Row 5: Divider -->
            <Border Grid.Row="5" Grid.ColumnSpan="2" Height="1" Margin="0,4,0,4"
                    Background="{ThemeResource DividerBrush}" />

            <!-- Row 6: Gesamt (bold) -->
            <TextBlock Grid.Row="6" Grid.Column="0" Text="Gesamt:" FontSize="13" FontWeight="SemiBold"
                       Foreground="{ThemeResource PrimaryTextBrush}" />
            <TextBlock Grid.Row="6" Grid.Column="1"
                       Text="{x:Bind ViewModel.StatisticsTotal, Mode=OneWay}"
                       FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Right"
                       Foreground="{ThemeResource PrimaryTextBrush}"
                       Visibility="{x:Bind local:MainView.InvertBool(ViewModel.IsAggregating), Mode=OneWay}" />
            <Border Grid.Row="6" Grid.Column="1" Height="13" Width="60" CornerRadius="4"
                    HorizontalAlignment="Right" Background="{ThemeResource ShimmerBaseBrush}"
                    Visibility="{x:Bind ViewModel.IsAggregating, Mode=OneWay,
                                Converter={StaticResource BoolToVisibilityConverter}}" />

            <!-- Row 7: Kosten (bold) -->
            <TextBlock Grid.Row="7" Grid.Column="0" Text="Kosten (API-Aqu.):" FontSize="13" FontWeight="SemiBold"
                       Foreground="{ThemeResource PrimaryTextBrush}" />
            <TextBlock Grid.Row="7" Grid.Column="1"
                       Text="{x:Bind ViewModel.StatisticsCost, Mode=OneWay}"
                       FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Right"
                       Foreground="{ThemeResource PrimaryTextBrush}"
                       Visibility="{x:Bind local:MainView.InvertBool(ViewModel.IsAggregating), Mode=OneWay}" />
            <Border Grid.Row="7" Grid.Column="1" Height="13" Width="60" CornerRadius="4"
                    HorizontalAlignment="Right" Background="{ThemeResource ShimmerBaseBrush}"
                    Visibility="{x:Bind ViewModel.IsAggregating, Mode=OneWay,
                                Converter={StaticResource BoolToVisibilityConverter}}" />

            <!-- Row 8: Burn Rate -->
            <TextBlock Grid.Row="8" Grid.Column="0" Text="Burn Rate:" FontSize="13"
                       Foreground="{ThemeResource SecondaryTextBrush}" />
            <TextBlock Grid.Row="8" Grid.Column="1"
                       Text="{x:Bind ViewModel.StatisticsBurnRate, Mode=OneWay}"
                       FontSize="13" HorizontalAlignment="Right"
                       Foreground="{ThemeResource SecondaryTextBrush}"
                       Visibility="{x:Bind local:MainView.InvertBool(ViewModel.IsAggregating), Mode=OneWay}" />
            <Border Grid.Row="8" Grid.Column="1" Height="13" Width="60" CornerRadius="4"
                    HorizontalAlignment="Right" Background="{ThemeResource ShimmerBaseBrush}"
                    Visibility="{x:Bind ViewModel.IsAggregating, Mode=OneWay,
                                Converter={StaticResource BoolToVisibilityConverter}}" />
        </Grid>
    </StackPanel>
    ```

    **Note on shimmer animation:** The shimmer `Border` elements use `ShimmerBaseBrush` as static background. For the actual animation (ColorAnimation from ShimmerBaseBrush to ShimmerHighlightBrush), add a Storyboard in code-behind that targets all shimmer borders. This is more reliable than trying to animate a ThemeResource in XAML.

    **Note on BoolToVisibilityConverter:** If `BoolToVisibilityConverter` doesn't already exist in the project's converters, add it. If the project uses a different pattern for bool-to-visibility (like `x:Bind` with a static helper), follow that pattern. The shimmer borders are visible when `IsAggregating=true`, the value TextBlocks are visible when `IsAggregating=false`. For the inverse visibility, use a static helper method `InvertBool` in MainView.xaml.cs or a `BoolToVisibilityInverterConverter`.

    **2. Update `MainView.xaml.cs`:**

    **2a.** Add shimmer animation logic. Create a `Storyboard` field and a method:
    ```csharp
    private Storyboard? _shimmerStoryboard;

    private void StartShimmerAnimation()
    {
        // Create animation targeting all shimmer Border backgrounds
        // ColorAnimation from #38383A to #555558, Duration 0.8s, AutoReverse, RepeatForever
        // Use Storyboard.SetTarget and Storyboard.SetTargetProperty
    }

    private void StopShimmerAnimation()
    {
        _shimmerStoryboard?.Stop();
    }
    ```

    **2b.** Subscribe to `ViewModel.PropertyChanged` in the page Loaded handler. When `IsAggregating` changes to true, call `StartShimmerAnimation()`. When false, call `StopShimmerAnimation()`.

    **2c.** Add static helper for inverse bool binding (if needed):
    ```csharp
    public static Visibility InvertBool(bool value) =>
        value ? Visibility.Collapsed : Visibility.Visible;
    ```

    **3. Update `SettingsView.xaml`:**
    Add two new rows at the bottom of the existing settings content, before the logout button:
    ```xml
    <!-- Pricing Info (COST-06) -->
    <Border Height="1" Background="{ThemeResource DividerBrush}" Margin="0,8,0,8" />
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Preisdaten:" FontSize="13"
                   Foreground="{ThemeResource SecondaryTextBrush}" />
        <TextBlock Grid.Row="0" Grid.Column="1"
                   Text="{x:Bind ViewModel.PricingSourceText}"
                   FontSize="13" HorizontalAlignment="Right"
                   Foreground="{ThemeResource PrimaryTextBrush}" />
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Zuletzt aktualisiert:" FontSize="13"
                   Foreground="{ThemeResource SecondaryTextBrush}" Margin="0,4,0,0" />
        <TextBlock Grid.Row="1" Grid.Column="1"
                   Text="{x:Bind ViewModel.LastPricingFetchText}"
                   FontSize="13" HorizontalAlignment="Right"
                   Foreground="{ThemeResource PrimaryTextBrush}" Margin="0,4,0,0" />
    </Grid>
    ```
  </action>
  <verify>
    <automated>dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj</automated>
  </verify>
  <acceptance_criteria>
    - CCInfoWindows/CCInfoWindows/Views/MainView.xaml contains `controls:Segmented` element
    - CCInfoWindows/CCInfoWindows/Views/MainView.xaml contains `STATISTIKEN` section header text
    - CCInfoWindows/CCInfoWindows/Views/MainView.xaml contains "Modelle:" label
    - CCInfoWindows/CCInfoWindows/Views/MainView.xaml contains "Eingabe:" label
    - CCInfoWindows/CCInfoWindows/Views/MainView.xaml contains "Ausgabe:" label
    - CCInfoWindows/CCInfoWindows/Views/MainView.xaml contains "Cache-Schreiben:" label
    - CCInfoWindows/CCInfoWindows/Views/MainView.xaml contains "Cache-Lesen:" label
    - CCInfoWindows/CCInfoWindows/Views/MainView.xaml contains "Gesamt:" label with FontWeight="SemiBold"
    - CCInfoWindows/CCInfoWindows/Views/MainView.xaml contains "Kosten" label with FontWeight="SemiBold"
    - CCInfoWindows/CCInfoWindows/Views/MainView.xaml contains "Burn Rate:" label
    - CCInfoWindows/CCInfoWindows/Views/MainView.xaml contains `ShimmerBaseBrush` references (shimmer borders)
    - CCInfoWindows/CCInfoWindows/Views/MainView.xaml does NOT contain old "TOKENS" section header
    - CCInfoWindows/CCInfoWindows/Views/MainView.xaml.cs contains shimmer animation code
    - CCInfoWindows/CCInfoWindows/Views/SettingsView.xaml contains "Preisdaten:" text
    - CCInfoWindows/CCInfoWindows/Views/SettingsView.xaml contains "Zuletzt aktualisiert:" text
    - `dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj` exits 0
  </acceptance_criteria>
  <done>STATISTIKEN section replaces TOKENS in MainView with Segmented tab bar, 8-row data table, shimmer placeholders. SettingsView shows pricing data source and last fetch time. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of STATISTIKEN section and settings pricing info</name>
  <files>CCInfoWindows/CCInfoWindows/Views/MainView.xaml, CCInfoWindows/CCInfoWindows/Views/SettingsView.xaml</files>
  <action>Human verifies the complete STATISTIKEN UI: tab bar styling, statistics table layout, shimmer animation, cost display formatting, burn rate display, and settings pricing info.</action>
  <what-built>
    Complete STATISTIKEN section: 4-segment tab bar (Session/Heute/Woche/Monat) with statistics table showing Modelle, Eingabe, Ausgabe, Cache-Schreiben, Cache-Lesen, Gesamt, Kosten, Burn Rate (computed via BurnRateCalculator). Shimmer animation on non-Session tabs during aggregation. Settings page with pricing data source display.
  </what-built>
  <how-to-verify>
    1. Launch the app: `dotnet run --project CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj`
    2. On the dashboard, verify the STATISTIKEN section appears below WOCHENLIMIT
    3. Verify the tab bar shows "Session / Heute / Woche / Monat" -- Session should be active by default
    4. If a session is selected, verify the statistics table shows token counts, costs, and burn rate (not just "0 T/h" if there is recent activity)
    5. Switch to "Heute" tab -- verify shimmer animation appears briefly, then values populate including burn rate
    6. Switch to "Woche" and "Monat" tabs -- same shimmer behavior
    7. Switch back to "Session" -- should load instantly without shimmer
    8. Check cost display: if model is recognized, cost should show without tilde. If not, should show "~$X.XX"
    9. Open Settings -- verify "Preisdaten:" shows "Live (LiteLLM API)" or "Fallback" and "Zuletzt aktualisiert:" shows timestamp or "Nie"
    10. Compare overall layout against `spec/v1.7.1/ccInfo-screenshot-macOS.png` for visual alignment
  </how-to-verify>
  <verify>Visual inspection by user</verify>
  <done>User approves STATISTIKEN section visual appearance, tab switching behavior, shimmer animation, cost formatting, burn rate display, and settings pricing display.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj` compiles clean
2. `dotnet test CCInfoWindows.Tests/CCInfoWindows.Tests.csproj -p:Platform=x64` all tests pass (including Plan 01 tests and new MainViewModelStatisticsTests)
3. Visual verification: tab bar, statistics table, shimmer, burn rate, settings pricing info all render correctly
</verification>

<success_criteria>
- CommunityToolkit.WinUI.Controls.Segmented NuGet package installed
- Theme resources added for segmented control and shimmer
- DI container registers IPricingService as singleton and JsonlService gets pricing injection
- MainViewModel has SelectedTabIndex, IsAggregating, and 8 statistics text properties
- Tab switching triggers GetStatistics with correct TimePeriod
- ApplyStatistics computes burn rate via BurnRateCalculator.ComputeBurnRate(stats.BurnRateEntries) -- no placeholder
- Session tab loads instantly; other tabs show shimmer during background aggregation
- TOKENS section fully replaced by STATISTIKEN section in MainView.xaml
- Shimmer animation runs on value positions during aggregation
- SettingsView shows pricing data source and last fetch time
- MainViewModelStatisticsTests verify tab-switch trigger logic and burn rate wiring
- All automated tests pass
- User approves visual appearance
</success_criteria>

<output>
After completion, create `.planning/phases/05-cost-analytics/05-02-SUMMARY.md`
</output>
