---
phase: 20-auth-flow-stability
plan: 03
type: execute
wave: 2
depends_on: [20-auth-flow-stability-01]
files_modified:
  - CCInfoWindows/CCInfoWindows/Views/LoginView.xaml
  - CCInfoWindows/CCInfoWindows/Views/LoginView.xaml.cs
  - CCInfoWindows/CCInfoWindows/ViewModels/LoginViewModel.cs
autonomous: true
requirements: [AUTH-06, AUTH-07]
decisions: [D-04, D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "D-04: LoginView displays a visible reload icon button as a Top-Right overlay over the existing `LoginWebView` (HorizontalAlignment=Right, VerticalAlignment=Top, Margin=8) at all times after the page is loaded."
    - "D-05: Reload button visual style matches the MainView footer refresh button — `FontIcon Glyph=&#xE72C;` with FontSize=16, Background=Transparent, BorderThickness=0, Padding=8 (UI-SPEC-locked override of the original D-05 prose), CornerRadius=6, Foreground=SecondaryTextBrush. Tooltip and AutomationProperties.Name bind via `l:Uids.Uid=\"LoginReloadButton\"` to the resw keys authored in Plan 01."
    - "D-06: Clicking the reload button invokes `LoginWebView?.CoreWebView2?.Reload()` with double null guard — clicking before WebView2 init is a silent no-op (no crash, no error UI). No try/catch, no retry, no busy state."
    - "D-07: After Logout (or any LoginView entry), `LoginWebView` is not visible until `NavigationCompleted` fires with `args.IsSuccess == true` AND `CoreWebView2.Source` starting with `https://claude.ai/login` — eliminating any flash of the previous chat URL."
    - "D-08: `IsLoading` remains the SINGLE source of truth for the WebView2/overlay visibility split: when `IsLoading == true` the loading overlay is visible and the WebView2 is collapsed (via `InvertedBoolToVisibilityConverter` from App.xaml:14); when `IsLoading == false` the WebView2 is visible and the overlay is collapsed. No second visibility flag exists (no `IsWebViewVisible` or similar)."
  artifacts:
    - path: "CCInfoWindows/CCInfoWindows/Views/LoginView.xaml"
      provides: "WinUI3Localizer xmlns import + LoginWebView Visibility bound to inverse of IsLoading + reload Button overlay (Z-order top)"
      contains: "InvertedBoolToVisibilityConverter"
    - path: "CCInfoWindows/CCInfoWindows/Views/LoginView.xaml.cs"
      provides: "OnReloadLoginClicked event handler (double null guard, no try/catch — ?. chain is the guard)"
      contains: "OnReloadLoginClicked"
    - path: "CCInfoWindows/CCInfoWindows/ViewModels/LoginViewModel.cs"
      provides: "Extended IsLoading semantics — flag stays true until login URL NavigationCompleted succeeds; the premature IsLoading=false at the InitializeWebViewAsync tail is removed"
      contains: "args.IsSuccess"
  key_links:
    - from: "LoginView.xaml reload Button (Click event)"
      to: "LoginView.xaml.cs OnReloadLoginClicked → CoreWebView2.Reload()"
      via: "WinUI 3 routed event handler with double null guard"
      pattern: "LoginWebView\\?\\.CoreWebView2\\?\\.Reload\\(\\)"
    - from: "LoginView.xaml l:Uids.Uid=\"LoginReloadButton\""
      to: "Strings/{en-US,de-DE}/Resources.resw LoginReloadButton.* keys"
      via: "WinUI3Localizer runtime tooltip + AutomationName resolution (Plan 01 Task 2 added the keys)"
      pattern: "l:Uids\\.Uid=\"LoginReloadButton\""
    - from: "LoginView.xaml LoginWebView Visibility binding"
      to: "LoginViewModel.IsLoading (via InvertedBoolToVisibilityConverter)"
      via: "Single-source-of-truth visibility gate per D-08 — IsLoading=true → Collapsed, IsLoading=false → Visible"
      pattern: "InvertedBoolToVisibilityConverter"
    - from: "LoginViewModel.HandleNavigationCompleted (extended)"
      to: "IsLoading = false (at the END of the success path, only when login URL loaded)"
      via: "args.IsSuccess + Source.StartsWith(https://claude.ai/login) gate (D-08)"
      pattern: "args\\.IsSuccess"
---

<objective>
Implement the LoginView UI delta: reload button overlay and post-logout WebView2 visibility gate. Three small, surgical edits across one XAML, one code-behind, one ViewModel. Depends on Plan 01 having authored the `LoginReloadButton.*` resw keys (otherwise the tooltip renders empty).

Honors locked decision **D-08**: extend the existing `IsLoading` ObservableProperty semantics so it stays `true` until the login-URL `NavigationCompleted` fires successfully — **no second visibility flag is added**. The existing global `InvertedBoolToVisibilityConverter` (registered in `App.xaml` line 14) is the inverse-binding helper; no new converter class is needed.

Purpose: Implements AUTH-06 (manual reload affordance) and AUTH-07 (no flash of previous chat URL after logout).
Output: Three files modified — visible icon button at LoginView top-right; WebView2 collapsed via inverse-`IsLoading` binding until login URL loaded; existing loading overlay (bound to `IsLoading` via `BoolToVisibilityConverter`) stays visible during the same window.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/20-auth-flow-stability/20-CONTEXT.md
@.planning/phases/20-auth-flow-stability/20-RESEARCH.md
@.planning/phases/20-auth-flow-stability/20-PATTERNS.md
@.planning/phases/20-auth-flow-stability/20-UI-SPEC.md
@.planning/phases/20-auth-flow-stability/20-01-SUMMARY.md

<interfaces>
<!-- Existing LoginView.xaml (full file, 38 lines) — Plan 03 extends. -->

```xml
<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CCInfoWindows.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <!-- Full-window WebView2 for claude.ai login -->
        <WebView2 x:Name="LoginWebView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

        <!-- Loading overlay -->
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
              Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" Width="48" Height="48"
                          HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>

        <!-- Error message -->
        <InfoBar Title="Error" Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                 IsOpen="{x:Bind ViewModel.HasErrorMessage, Mode=OneWay}"
                 Severity="Error" VerticalAlignment="Top" Margin="8" />
    </Grid>
</Page>
```

<!-- The xmlns import to add — copied from MainView.xaml. -->

```xml
xmlns:l="using:WinUI3Localizer"
```

<!-- The MainView footer refresh button visual reference (mirror this style for the reload button).
     Source: CCInfoWindows/CCInfoWindows/Views/MainView.xaml (FooterRefreshButton block) -->

```xml
<Button l:Uids.Uid="FooterRefreshButton"
        ToolTipService.ToolTip="Refresh"
        Command="{x:Bind ViewModel.RefreshCommand}"
        Background="Transparent" BorderThickness="0"
        Padding="8" CornerRadius="6">
    <FontIcon x:Name="RefreshIcon" Glyph="&#xE895;" FontSize="16"
              Foreground="{ThemeResource SecondaryTextBrush}" ... />
</Button>
```

<!-- The pre-existing inverse converter (App-level resource) Plan 03 uses for the WebView2 Visibility binding.
     Source: CCInfoWindows/CCInfoWindows/Converters/InvertedBoolToVisibilityConverter.cs
             registered in CCInfoWindows/CCInfoWindows/App.xaml line 14:
             <converters:InvertedBoolToVisibilityConverter x:Key="InvertedBoolToVisibilityConverter" />
     Behavior: bool value true → Visibility.Collapsed; false → Visibility.Visible. -->

<!-- The current LoginViewModel.HandleNavigationCompleted to extend.
     Source: CCInfoWindows/CCInfoWindows/ViewModels/LoginViewModel.cs (HandleNavigationCompleted method) -->

```csharp
public async void HandleNavigationCompleted(WebView2 sender, CoreWebView2NavigationCompletedEventArgs args)
{
    try
    {
        if (_loginHandled || sender.CoreWebView2 is null) return;
        await TryExtractSessionCookieAsync(sender.CoreWebView2, sender.CoreWebView2.Source ?? "");
    }
    catch (Exception ex) { ErrorMessage = "Login processing failed."; ... }
}
```

<!-- The current LoginViewModel.InitializeWebViewAsync sets `IsLoading = false;` at the tail (the line
     immediately after `webView.CoreWebView2.Navigate("https://claude.ai/login");`). Plan 03 Task 3
     REMOVES that line — IsLoading must stay `true` until the login-URL NavigationCompleted succeeds. -->
```csharp
webView.CoreWebView2.Navigate("https://claude.ai/login");
IsLoading = false;   // ← REMOVE this line; the new gate in HandleNavigationCompleted owns the false-flip.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add WinUI3Localizer xmlns + reload button overlay + inverse-IsLoading Visibility binding on LoginWebView (LoginView.xaml)</name>
  <files>CCInfoWindows/CCInfoWindows/Views/LoginView.xaml</files>
  <read_first>
    - CCInfoWindows/CCInfoWindows/Views/LoginView.xaml (current full file, 38 lines)
    - CCInfoWindows/CCInfoWindows/Views/MainView.xaml (top of file for the WinUI3Localizer xmlns import; the FooterRefreshButton block for the visual reference)
    - CCInfoWindows/CCInfoWindows/App.xaml (lines 13-14 confirm both `BoolToVisibilityConverter` AND `InvertedBoolToVisibilityConverter` are registered as App-level resources — no new converter needed)
    - CCInfoWindows/CCInfoWindows/Converters/InvertedBoolToVisibilityConverter.cs (verify behavior: `true → Collapsed`, `false → Visible` — this is exactly the D-08 semantics for `IsLoading`)
    - .planning/phases/20-auth-flow-stability/20-PATTERNS.md (Pattern Map for LoginView.xaml — reload button visual, FooterRefreshButton analog)
    - .planning/phases/20-auth-flow-stability/20-UI-SPEC.md (locked component inventory; CornerRadius=6, Padding=8, FontSize=16, Glyph=&#xE72C;)
  </read_first>
  <action>
Replace the full content of `CCInfoWindows/CCInfoWindows/Views/LoginView.xaml` with the following (38 → ~50 lines). Three deltas: (a) add `xmlns:l="using:WinUI3Localizer"`, (b) bind `LoginWebView.Visibility` to the INVERSE of `IsLoading` via the existing global `InvertedBoolToVisibilityConverter`, (c) append the reload button as the LAST child of the root `Grid` (Z-order top per D-04). Use the Write/Edit tool — DO NOT use heredoc.

```xml
<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CCInfoWindows.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:l="using:WinUI3Localizer"
    mc:Ignorable="d">

    <Grid>
        <!-- Full-window WebView2 for claude.ai login.
             D-07/D-08: Visibility is bound to the INVERSE of IsLoading. The flag stays true
             (so this WebView2 stays Collapsed) until LoginViewModel.HandleNavigationCompleted
             fires for the login URL with args.IsSuccess == true (Task 3). The existing loading
             overlay is bound to IsLoading directly (BoolToVisibilityConverter) — they are
             two views of the SAME flag, so no second visibility property exists. -->
        <WebView2
            x:Name="LoginWebView"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource InvertedBoolToVisibilityConverter}}" />

        <!-- Loading overlay — visible while IsLoading == true (single source of truth). -->
        <Grid
            Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
            Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <ProgressRing
                IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                Width="48"
                Height="48"
                HorizontalAlignment="Center"
                VerticalAlignment="Center" />
        </Grid>

        <!-- Error message -->
        <InfoBar
            Title="Error"
            Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
            IsOpen="{x:Bind ViewModel.HasErrorMessage, Mode=OneWay}"
            Severity="Error"
            VerticalAlignment="Top"
            Margin="8" />

        <!-- D-04 / D-05: Reload button overlay (top-right). Visual mirrors MainView footer FooterRefreshButton.
             Glyph &#xE72C; (Reload) is distinct from MainView's &#xE895; (ArrowSync) — semantically correct.
             l:Uids.Uid resolves LoginReloadButton.* resw keys (Plan 01 Task 2 added them in en-US + de-DE). -->
        <Button l:Uids.Uid="LoginReloadButton"
                Click="OnReloadLoginClicked"
                Background="Transparent"
                BorderThickness="0"
                Padding="8"
                CornerRadius="6"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="8">
            <FontIcon Glyph="&#xE72C;"
                      FontSize="16"
                      Foreground="{ThemeResource SecondaryTextBrush}" />
        </Button>
    </Grid>
</Page>
```

Strict rules:
- The new `xmlns:l="using:WinUI3Localizer"` MUST be added — without it, `l:Uids.Uid` is undefined and the file fails to compile.
- `LoginWebView.Visibility` MUST use `Converter={StaticResource InvertedBoolToVisibilityConverter}` — NOT a literal `Visibility="Collapsed"`, NOT a `ConverterParameter=Invert` hack, NOT a second visibility flag. The `InvertedBoolToVisibilityConverter` is already registered as an App-level resource (`App.xaml` line 14) and produces `Collapsed` when `IsLoading == true` and `Visible` when `IsLoading == false` — exactly the D-08 semantics. This honors the locked decision: `IsLoading` is the SINGLE source of truth.
- The reload button MUST be the LAST child of the root `Grid` (Z-order overlay). Do NOT reorder existing children.
- The `Click="OnReloadLoginClicked"` event handler is wired to the code-behind in Task 2 — do NOT add `Command=...` (the click target needs direct WebView2 access per CONTEXT D-06).
- Glyph `&#xE72C;` is `Reload` (NOT `&#xE895;` ArrowSync — UI-SPEC explicitly distinguishes page-reload vs. data-refresh semantics).
- DO NOT introduce `IsWebViewVisible` or any other new ObservableProperty — D-08 is locked: "no second visibility flag needed".
  </action>
  <verify>
    <automated>dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj</automated>
  </verify>
  <acceptance_criteria>
    - File contains the localizer xmlns: `grep -F 'xmlns:l="using:WinUI3Localizer"' CCInfoWindows/CCInfoWindows/Views/LoginView.xaml` returns 1 match.
    - Reload button uses correct Uid: `grep -F 'l:Uids.Uid="LoginReloadButton"' CCInfoWindows/CCInfoWindows/Views/LoginView.xaml` returns 1 match.
    - Reload glyph is `&#xE72C;` (NOT `&#xE895;`): `grep -F 'Glyph="&#xE72C;"' CCInfoWindows/CCInfoWindows/Views/LoginView.xaml` returns 1 match AND `grep -F 'Glyph="&#xE895;"' CCInfoWindows/CCInfoWindows/Views/LoginView.xaml` returns 0 matches.
    - Click handler reference present: `grep -F 'Click="OnReloadLoginClicked"' CCInfoWindows/CCInfoWindows/Views/LoginView.xaml` returns 1 match.
    - LoginWebView Visibility uses the inverse-IsLoading binding (D-08 single-source-of-truth): `grep -F 'InvertedBoolToVisibilityConverter' CCInfoWindows/CCInfoWindows/Views/LoginView.xaml` returns 1 match (used by the LoginWebView Visibility binding).
    - **No second visibility flag introduced (D-08 negative-presence check):** `grep -c 'IsWebViewVisible' CCInfoWindows/CCInfoWindows/Views/LoginView.xaml` == 0.
    - **No literal Visibility="Collapsed" on LoginWebView:** `grep -F 'Visibility="Collapsed"' CCInfoWindows/CCInfoWindows/Views/LoginView.xaml` returns 0 matches (the only Visibility-related markup on `LoginWebView` is the inverse-converter binding).
    - No `Command=` on the reload button (would defeat code-behind-direct-access pattern): `grep -B2 -A8 'l:Uids.Uid="LoginReloadButton"' CCInfoWindows/CCInfoWindows/Views/LoginView.xaml | grep -c 'Command='` == 0.
    - Reload button alignment matches D-04: `grep -B2 -A12 'l:Uids.Uid="LoginReloadButton"' CCInfoWindows/CCInfoWindows/Views/LoginView.xaml | grep -F 'HorizontalAlignment="Right"'` returns 1 match AND same range contains `VerticalAlignment="Top"` AND `Margin="8"`.
    - Visual coherence per UI-SPEC: button has `Padding="8"`, `CornerRadius="6"`, `BorderThickness="0"`, `Background="Transparent"` — `grep -B2 -A12 'l:Uids.Uid="LoginReloadButton"' CCInfoWindows/CCInfoWindows/Views/LoginView.xaml` shows all four.
    - FontIcon size matches MainView footer: `grep -B2 -A4 'Glyph="&#xE72C;"' CCInfoWindows/CCInfoWindows/Views/LoginView.xaml | grep -F 'FontSize="16"'` returns 1 match.
    - Project still compiles (XAML schema valid): `dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj` exits 0 — note this requires Tasks 2 and 3 to be applied as well; if isolated, accept compile errors ONLY for missing `OnReloadLoginClicked` reference and re-verify after Wave 2 task chain completes.
  </acceptance_criteria>
  <done>LoginView.xaml has the WinUI3Localizer import, the reload button overlay (top-right, locked visual style), and the inverse-IsLoading binding on `LoginWebView.Visibility`. No second visibility flag was introduced. The XAML schema is valid (verified by build after Tasks 2-3 land).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add OnReloadLoginClicked handler to LoginView.xaml.cs (double null guard, no try/catch)</name>
  <files>CCInfoWindows/CCInfoWindows/Views/LoginView.xaml.cs</files>
  <read_first>
    - CCInfoWindows/CCInfoWindows/Views/LoginView.xaml.cs (current 38 lines — sibling pattern is the existing `OnLoaded` handler)
    - .planning/phases/20-auth-flow-stability/20-PATTERNS.md (LoginView.xaml.cs section — exact handler shape)
    - .planning/phases/20-auth-flow-stability/20-RESEARCH.md (Pitfall 1 — `CoreWebView2` is null until `EnsureCoreWebView2Async` resolves)
  </read_first>
  <action>
Add the `OnReloadLoginClicked` method as a sibling of the existing `OnLoaded` method in `CCInfoWindows/CCInfoWindows/Views/LoginView.xaml.cs`. Insert immediately after the closing brace of `OnLoaded` and before the closing brace of the `LoginView` class. Match local indentation (4 spaces).

```csharp
    /// <summary>
    /// D-06: Manual reload of the login page. Double null guard handles
    /// the early-click case where CoreWebView2 has not yet been initialized
    /// by EnsureCoreWebView2Async (Pitfall 1). One-shot — no retry, no busy state.
    /// </summary>
    private void OnReloadLoginClicked(object sender, RoutedEventArgs e)
    {
        LoginWebView?.CoreWebView2?.Reload();
    }
```

Strict rules:
- DO NOT wrap the call in try/catch — the `?.` chain IS the guard. Per D-06 there is no error UI for this path.
- The signature MUST be `(object sender, RoutedEventArgs e)` — `RoutedEventArgs` is the `Microsoft.UI.Xaml.RoutedEventArgs` already imported at the top of the file (`using Microsoft.UI.Xaml;`). Do NOT add `async` (Reload is fire-and-forget native).
- DO NOT modify `OnLoaded` or the constructor.
- DO NOT add new using directives — `LoginWebView` is the x:Name from XAML; `RoutedEventArgs` and the WebView2 type chain are already in scope via existing usings.
  </action>
  <verify>
    <automated>dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj</automated>
  </verify>
  <acceptance_criteria>
    - Method exists with exact signature: `grep -F 'private void OnReloadLoginClicked(object sender, RoutedEventArgs e)' CCInfoWindows/CCInfoWindows/Views/LoginView.xaml.cs` returns 1 match.
    - Body invokes Reload with double null guard: `grep -F 'LoginWebView?.CoreWebView2?.Reload();' CCInfoWindows/CCInfoWindows/Views/LoginView.xaml.cs` returns 1 match.
    - No try/catch wraps the call: `grep -B1 -A2 'OnReloadLoginClicked' CCInfoWindows/CCInfoWindows/Views/LoginView.xaml.cs | grep -c 'try {'` == 0.
    - Method is sibling of OnLoaded (not nested): `grep -cE "^    private (async )?void On(Loaded|ReloadLoginClicked)" CCInfoWindows/CCInfoWindows/Views/LoginView.xaml.cs` == 2.
    - File still has its single `partial class` declaration (no accidental refactor): `grep -cE "public sealed partial class LoginView" CCInfoWindows/CCInfoWindows/Views/LoginView.xaml.cs` == 1.
    - Project compiles: `dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj` exits 0 (after Task 1 + Task 3 also applied).
  </acceptance_criteria>
  <done>LoginView code-behind has a sibling `OnReloadLoginClicked` method with the exact double-null-guard shape; the XAML `Click="OnReloadLoginClicked"` resolves correctly; no try/catch was added.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend HandleNavigationCompleted in LoginViewModel.cs to flip IsLoading=false ONLY on login-URL success (D-08); remove the premature IsLoading=false at the InitializeWebViewAsync tail</name>
  <files>CCInfoWindows/CCInfoWindows/ViewModels/LoginViewModel.cs</files>
  <read_first>
    - CCInfoWindows/CCInfoWindows/ViewModels/LoginViewModel.cs (full file — IsLoading ObservableProperty declaration; the `IsLoading = false;` line at the InitializeWebViewAsync tail; the HandleNavigationCompleted method to extend; the WebView2-init failure path that already sets `IsLoading = false;`)
    - CCInfoWindows/CCInfoWindows/Views/LoginView.xaml (Plan 03 Task 1) — confirms LoginWebView Visibility is bound to inverse of IsLoading (D-08 single source of truth)
    - .planning/phases/20-auth-flow-stability/20-PATTERNS.md (LoginViewModel section — IsLoading semantics extension)
    - .planning/phases/20-auth-flow-stability/20-RESEARCH.md (D-08; Pitfall 4 — args.IsSuccess check is mandatory)
    - .planning/phases/20-auth-flow-stability/20-UI-SPEC.md (interaction state machine for LoginView visibility)
  </read_first>
  <action>
Apply two atomic edits to `CCInfoWindows/CCInfoWindows/ViewModels/LoginViewModel.cs`. **No new field is introduced** — D-08 is locked: extend the existing `IsLoading` ObservableProperty semantics, do NOT add a second visibility flag.

**Edit 1 — Extend `HandleNavigationCompleted` to flip `IsLoading = false` ONLY when the login URL has loaded successfully (D-08).** Replace the body of the existing `HandleNavigationCompleted` method wholesale. The `_loginHandled` cookie-extraction de-bouncer at the top of the method MUST be preserved (it is a separate concern from the visibility gate).

Final replacement body for `HandleNavigationCompleted`:

```csharp
    /// <summary>
    /// Handles full page navigation completion.
    /// D-08: extend IsLoading semantics — keep IsLoading=true (loading overlay visible,
    /// LoginWebView Collapsed via inverse binding in LoginView.xaml) until the login URL
    /// itself has loaded successfully. Single source of truth, no second visibility flag.
    /// args.IsSuccess guards against offline/error completions (Pitfall 4).
    /// </summary>
    public async void HandleNavigationCompleted(WebView2 sender, CoreWebView2NavigationCompletedEventArgs args)
    {
        try
        {
            if (_loginHandled || sender.CoreWebView2 is null) return;

            var source = sender.CoreWebView2.Source ?? string.Empty;

            // D-08: reveal the WebView2 (and hide the loading overlay) only when the login URL
            // has finished loading successfully — prevents AUTH-07 flash of any cached chat URL.
            if (args.IsSuccess &&
                source.StartsWith("https://claude.ai/login", StringComparison.OrdinalIgnoreCase))
            {
                IsLoading = false;
            }

            await TryExtractSessionCookieAsync(sender.CoreWebView2, source);
        }
        catch (Exception ex)
        {
            ErrorMessage = "Login processing failed.";
            System.Diagnostics.Debug.WriteLine($"[LoginViewModel] HandleNavigationCompleted: {ex.Message}");
        }
    }
```

**Edit 2 — Remove the premature `IsLoading = false;` at the `InitializeWebViewAsync` tail.** Locate the line `IsLoading = false;` that sits IMMEDIATELY AFTER `webView.CoreWebView2.Navigate("https://claude.ai/login");` inside the `InitializeWebViewAsync` method (this is the LAST executable statement of `InitializeWebViewAsync` before the closing brace). DELETE that single line. Per D-08 the loading overlay must remain visible until `HandleNavigationCompleted` confirms the login URL loaded successfully — this premature flip violates the new contract.

Final shape of the `InitializeWebViewAsync` tail after edit:

```csharp
        // Register SourceChanged on CoreWebView2 — fires on SPA pushState navigation too.
        // NavigationCompleted only fires on full page loads, which misses SPA route changes.
        webView.CoreWebView2.SourceChanged += HandleSourceChanged;
        webView.CoreWebView2.HistoryChanged += HandleHistoryChanged;
        webView.NavigationCompleted += HandleNavigationCompleted;
        webView.CoreWebView2.Navigate("https://claude.ai/login");
        // D-08: IsLoading stays true here. HandleNavigationCompleted flips it to false
        // ONLY when args.IsSuccess && Source starts with https://claude.ai/login. This
        // keeps LoginWebView (bound to inverse of IsLoading) Collapsed and the loading
        // overlay (bound to IsLoading) Visible — preventing AUTH-07 flash of cached chat URL.
    }
```

The OTHER existing `IsLoading = false;` site (inside the WebView2-init failure path — the `catch (Exception retryEx) { ... IsLoading = false; return; }` block in `InitializeWebViewAsync`) MUST be preserved. That site handles the unrecoverable WebView2 initialization failure and must continue to clear the loading state so the user is not stuck on a permanent ProgressRing.

Strict rules:
- **NO new field.** Do NOT introduce `IsWebViewVisible`, `IsWebViewReady`, or any other ObservableProperty. D-08 is LOCKED to the IsLoading-extension approach. (RESEARCH §Anti-Patterns line 288 explicitly forbids "Adding a second visibility flag on `LoginWebView`".)
- The `args.IsSuccess` check is mandatory per D-08 / Pitfall 4 — without it, an offline failure would falsely reveal a broken page.
- The `StringComparison.OrdinalIgnoreCase` is mandatory — claude.ai redirect URLs are case-insensitive.
- Do NOT delete the `_loginHandled` guard at the top of `HandleNavigationCompleted` — it remains the cookie-extraction de-bouncer (separate concern from the new visibility gate).
- Do NOT modify `TryExtractSessionCookieAsync`, `IsPostLoginUrl`, `HandleSourceChanged`, or `HandleHistoryChanged`.
- The `IsLoading = false;` deletion at the `InitializeWebViewAsync` tail is the ONLY behavioral edit inside `InitializeWebViewAsync` — do NOT touch any other line there. The replacement comment block is documentation only.
  </action>
  <verify>
    <automated>dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj</automated>
  </verify>
  <acceptance_criteria>
    - **No second visibility flag is introduced (D-08 negative-presence — file-wide):** `grep -c 'IsWebViewVisible' CCInfoWindows/CCInfoWindows/ViewModels/LoginViewModel.cs` == 0 AND `grep -c '_isWebViewVisible' CCInfoWindows/CCInfoWindows/ViewModels/LoginViewModel.cs` == 0.
    - **`args.IsSuccess` check present in HandleNavigationCompleted:** `grep -F 'args.IsSuccess' CCInfoWindows/CCInfoWindows/ViewModels/LoginViewModel.cs` returns 1 match.
    - **Login URL prefix check uses correct comparison:** `grep -F 'StartsWith("https://claude.ai/login", StringComparison.OrdinalIgnoreCase)' CCInfoWindows/CCInfoWindows/ViewModels/LoginViewModel.cs` returns 1 match.
    - **`IsLoading = false;` appears EXACTLY twice in the whole file** (the WebView2-init failure path inside `InitializeWebViewAsync`'s outer catch + the new HandleNavigationCompleted gate). Comment lines are stripped before counting to avoid the self-invalidating-grep-gate antipattern: `grep -v '^[[:space:]]*//' CCInfoWindows/CCInfoWindows/ViewModels/LoginViewModel.cs | grep -cE "^[[:space:]]*IsLoading = false;[[:space:]]*$"` == 2.
    - **Negative-presence check — the InitializeWebViewAsync tail no longer contains `IsLoading = false;`:** Extract the InitializeWebViewAsync method body and assert no `IsLoading = false;` survives between the `webView.CoreWebView2.Navigate("https://claude.ai/login");` line and the closing brace of the method. Recommended check (POSIX awk, single line): `awk '/webView\\.CoreWebView2\\.Navigate\\("https:\\/\\/claude\\.ai\\/login"\\);/{p=1;next} p && /^    \\}/{p=0} p && /^[[:space:]]*IsLoading = false;[[:space:]]*$/{found=1} END{exit found?1:0}' CCInfoWindows/CCInfoWindows/ViewModels/LoginViewModel.cs` exits 0 (i.e. NO match in that range). Fallback if awk unavailable on the executor: visually inspect the InitializeWebViewAsync tail in the diff and confirm the line is absent.
    - **`_loginHandled` guard is preserved:** `grep -F 'if (_loginHandled || sender.CoreWebView2 is null) return;' CCInfoWindows/CCInfoWindows/ViewModels/LoginViewModel.cs` returns 1 match.
    - **No new using directives:** `grep -cE "^using " CCInfoWindows/CCInfoWindows/ViewModels/LoginViewModel.cs` == count from before edit (no new imports needed; `StringComparison` is in `System` which is implicit in .NET 9).
    - **No new ObservableProperty introduced — only the existing two:** `grep -cE "^[[:space:]]*\[ObservableProperty\][[:space:]]*$" CCInfoWindows/CCInfoWindows/ViewModels/LoginViewModel.cs` == 2 (the existing `_isLoading` + `_errorMessage`, unchanged).
    - **Project compiles end-to-end** (LoginView.xaml inverse-binding + code-behind handler + extended `HandleNavigationCompleted` all align): `dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj` exits 0.
    - **Wave 0 unit tests do not regress** (this VM is unrelated): `dotnet test CCInfoWindows.Tests/CCInfoWindows.Tests.csproj --filter "FullyQualifiedName~MainViewModelAuthFlow" --no-build` reports 4/4 PASS (assumes Plan 02 already shipped — Wave 2 ordering).
  </acceptance_criteria>
  <done>LoginViewModel's `HandleNavigationCompleted` flips `IsLoading = false` only when the login URL succeeds, and the premature `IsLoading = false;` after the `Navigate(...)` call is gone. No second visibility flag was introduced — `IsLoading` remains the single source of truth per D-08. The full app compiles and renders LoginView correctly with the loading overlay → reveal flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView2 navigation events → ViewModel state | `args.IsSuccess` and `CoreWebView2.Source` are inputs from the rendering engine. URL prefix check guards against arbitrary redirect targets revealing the WebView2. |
| User click on reload button → CoreWebView2 native API | `CoreWebView2.Reload()` is a parameterless native call; no untrusted input flows in. |
| resw localization keys → Tooltip/Narrator | Static strings only — no formatting placeholders, no user content interpolation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03-01 | Information Disclosure | Stale chat URL flash on logout | mitigate | D-07/D-08 — `LoginWebView.Visibility` bound to inverse of `IsLoading`; flag stays true until `HandleNavigationCompleted` fires for the login URL with `args.IsSuccess == true` AND `StartsWith("https://claude.ai/login", OrdinalIgnoreCase)`. User never sees pre-purge chat content. Verified manually per VALIDATION smoke checklist row 1. |
| T-20-03-02 | Tampering | Reload button XSS / script injection | mitigate | `CoreWebView2.Reload()` is a parameterless native API — no user input is ever passed. The button's `Click` handler is the only entry point. Verified by grep: handler body is exactly `LoginWebView?.CoreWebView2?.Reload();`. |
| T-20-03-03 | Denial of Service | Repeated reload click before WebView2 init | mitigate | Double `?.` null-guard on `LoginWebView` and `CoreWebView2`. Pre-init clicks become silent no-ops (Pitfall 1 mitigation). |
| T-20-03-04 | Information Disclosure | Spoofed redirect to non-claude.ai login URL | mitigate | URL prefix check uses `StartsWith("https://claude.ai/login", OrdinalIgnoreCase)` — only claude.ai's literal login URL flips `IsLoading = false`. Any third-party redirect (e.g., open redirect attack) keeps `IsLoading = true` and the WebView2 stays Collapsed. |
| T-20-03-05 | Denial of Service | Login URL never loads (offline) | accept | `IsLoading` stays true; loading overlay stays visible; reload button remains clickable as the user-facing recovery path (per CONTEXT §Claude's Discretion: no NavigationFailed timeout fallback for v1.4). |
| T-20-03-06 | Tampering | Localization key swap (resw replaced by hostile party) | accept | Resw files ship in the signed binary; tampering requires write access to `Program Files\CCInfoWindows`. Out of threat surface for this phase. |
</threat_model>

<verification>
- `dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj` exits 0 (covers XAML schema + code-behind + ViewModel as one unit).
- `dotnet test CCInfoWindows.Tests/CCInfoWindows.Tests.csproj` shows the 4 AUTH-flow tests still GREEN (Plan 02 dependency) and no new regressions.
- Manual smoke (deferred to Plan 04 verification step): logout flow shows ProgressRing only, then the login form — no flash of chat URL.
- Manual smoke: reload button click triggers WebView2 reload (visible re-render of the login form).
- Manual smoke: tooltip text matches system language (EN: "Reload page" / DE: "Seite neu laden"); Narrator announces "Reload login page" / "Login-Seite neu laden".
- Decision-fidelity check: D-08 is honored — no `IsWebViewVisible` field anywhere in the project, `LoginWebView.Visibility` derives directly from `IsLoading` via the existing global `InvertedBoolToVisibilityConverter`.
</verification>

<success_criteria>
- LoginView.xaml renders the reload button at top-right with the locked visual style (matches MainView footer refresh button).
- LoginWebView is hidden (Collapsed via inverse-IsLoading binding) until the login URL has loaded successfully.
- Click handler in code-behind calls `CoreWebView2.Reload()` with double null guard.
- LoginViewModel's `HandleNavigationCompleted` flips `IsLoading=false` only on the login-URL success path; the premature `IsLoading=false` at the InitializeWebViewAsync tail is removed.
- **D-08 honored:** no second visibility flag exists; `IsLoading` is the single source of truth for both the loading overlay and the WebView2 visibility gate.
- No regressions in the 4 AUTH-flow unit tests from Plan 02.
- Project builds; resw lookups for `LoginReloadButton.*` resolve correctly (Plan 01 dependency).
</success_criteria>

<output>
After completion, create `.planning/phases/20-auth-flow-stability/20-03-SUMMARY.md` with:
- Files modified (3 — LoginView.xaml, LoginView.xaml.cs, LoginViewModel.cs)
- Lines added/removed counts
- Decision provenance: D-04, D-05, D-06, D-07, D-08 cited in code comments
- Confirmation that D-08 was honored: `IsLoading` extended (single flag), `LoginWebView.Visibility` bound to inverse via the pre-existing global `InvertedBoolToVisibilityConverter`. No `IsWebViewVisible` field exists anywhere in the project.
- Open question for Plan 04: smoke test verifies that `Window.Activate()` actually unminimizes (Open Question #2 / Assumption A4)
</output>
