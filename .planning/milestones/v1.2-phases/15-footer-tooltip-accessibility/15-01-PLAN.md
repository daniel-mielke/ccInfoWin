---
phase: 15-footer-tooltip-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [ACC-01, ACC-02, ACC-03]

must_haves:
  truths:
    - "User sees localized tooltip when hovering each footer button (Refresh, Settings, Quit)"
    - "User's screen reader announces the purpose of each footer button via AutomationProperties.Name"
    - "User sees tooltip text in the currently selected app language (de-DE or en-US)"
  artifacts:
    - path: "CCInfoWindows/CCInfoWindows/Views/MainView.xaml"
      provides: "Footer buttons with l:Uids.Uid for WinUI3Localizer"
      contains: "l:Uids.Uid=\"FooterRefreshButton\""
    - path: "CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw"
      provides: "English tooltip and automation property strings for all 3 footer buttons"
      contains: "FooterRefreshButton.[using:Microsoft.UI.Xaml.Controls]ToolTipService.ToolTip"
    - path: "CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw"
      provides: "German tooltip and automation property strings for all 3 footer buttons"
      contains: "FooterRefreshButton.[using:Microsoft.UI.Xaml.Controls]ToolTipService.ToolTip"
  key_links:
    - from: "CCInfoWindows/CCInfoWindows/Views/MainView.xaml"
      to: "CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw"
      via: "WinUI3Localizer l:Uids.Uid property-path resolution"
      pattern: "l:Uids\\.Uid=\"Footer(Refresh|Settings|Quit)Button\""
    - from: "CCInfoWindows/CCInfoWindows/Views/MainView.xaml"
      to: "CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw"
      via: "WinUI3Localizer l:Uids.Uid property-path resolution"
      pattern: "l:Uids\\.Uid=\"Footer(Refresh|Settings|Quit)Button\""
---

<objective>
Verify that footer button tooltips and accessibility properties are correctly wired and functional.

Purpose: Research confirms the implementation is already complete — XAML buttons have `l:Uids.Uid` set, and both resw files contain all 6 required entries (3 ToolTipService.ToolTip + 3 AutomationProperties.Name). This plan confirms the wiring works at runtime via build verification and manual UI testing.

Output: Clean build confirming no regressions; human-verified tooltip behavior and screen reader accessibility on all three footer buttons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-footer-tooltip-accessibility/15-RESEARCH.md

@CCInfoWindows/CCInfoWindows/Views/MainView.xaml
@CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw
@CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw

<interfaces>
<!-- Implementation is already complete. No code changes expected. These are the existing artifacts to verify. -->

From MainView.xaml (lines 586, 609, 618) — footer buttons with Uid:
```xml
<Button l:Uids.Uid="FooterRefreshButton" Command="{x:Bind ViewModel.RefreshCommand}" ...>
<Button l:Uids.Uid="FooterSettingsButton" Command="{x:Bind ViewModel.OpenSettingsCommand}" ...>
<Button l:Uids.Uid="FooterQuitButton" Command="{x:Bind ViewModel.ExitAppCommand}" ...>
```

From en-US/Resources.resw (lines 101-118) — all 6 entries:
```xml
FooterRefreshButton.[using:Microsoft.UI.Xaml.Controls]ToolTipService.ToolTip = Refresh
FooterRefreshButton.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name = Refresh
FooterSettingsButton.[using:Microsoft.UI.Xaml.Controls]ToolTipService.ToolTip = Settings
FooterSettingsButton.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name = Settings
FooterQuitButton.[using:Microsoft.UI.Xaml.Controls]ToolTipService.ToolTip = Quit
FooterQuitButton.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name = Quit
```

From de-DE/Resources.resw (lines 101-118) — all 6 entries:
```xml
FooterRefreshButton.[using:Microsoft.UI.Xaml.Controls]ToolTipService.ToolTip = Aktualisieren
FooterRefreshButton.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name = Aktualisieren
FooterSettingsButton.[using:Microsoft.UI.Xaml.Controls]ToolTipService.ToolTip = Einstellungen
FooterSettingsButton.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name = Einstellungen
FooterQuitButton.[using:Microsoft.UI.Xaml.Controls]ToolTipService.ToolTip = Beenden
FooterQuitButton.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name = Beenden
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and resw/XAML integrity check</name>
  <files>CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj</files>
  <read_first>
    CCInfoWindows/CCInfoWindows/Views/MainView.xaml
    CCInfoWindows/CCInfoWindows/Strings/en-US/Resources.resw
    CCInfoWindows/CCInfoWindows/Strings/de-DE/Resources.resw
  </read_first>
  <action>
    Verify that the existing implementation is correct and complete WITHOUT making any code changes:

    1. Run `dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj` — confirm clean build with 0 errors.

    2. Verify MainView.xaml footer buttons have correct Uid values:
       - Line ~586: `l:Uids.Uid="FooterRefreshButton"` on the Refresh Button element (inside the Grid wrapper)
       - Line ~609: `l:Uids.Uid="FooterSettingsButton"` on the Settings Button element
       - Line ~618: `l:Uids.Uid="FooterQuitButton"` on the Quit Button element

    3. Verify en-US/Resources.resw contains exactly these 6 entries (3 ToolTip + 3 AutomationProperties):
       - `FooterRefreshButton.[using:Microsoft.UI.Xaml.Controls]ToolTipService.ToolTip` = `Refresh`
       - `FooterRefreshButton.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name` = `Refresh`
       - `FooterSettingsButton.[using:Microsoft.UI.Xaml.Controls]ToolTipService.ToolTip` = `Settings`
       - `FooterSettingsButton.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name` = `Settings`
       - `FooterQuitButton.[using:Microsoft.UI.Xaml.Controls]ToolTipService.ToolTip` = `Quit`
       - `FooterQuitButton.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name` = `Quit`

    4. Verify de-DE/Resources.resw contains exactly these 6 entries:
       - `FooterRefreshButton.[using:Microsoft.UI.Xaml.Controls]ToolTipService.ToolTip` = `Aktualisieren`
       - `FooterRefreshButton.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name` = `Aktualisieren`
       - `FooterSettingsButton.[using:Microsoft.UI.Xaml.Controls]ToolTipService.ToolTip` = `Einstellungen`
       - `FooterSettingsButton.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name` = `Einstellungen`
       - `FooterQuitButton.[using:Microsoft.UI.Xaml.Controls]ToolTipService.ToolTip` = `Beenden`
       - `FooterQuitButton.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name` = `Beenden`

    5. Run the full test suite `dotnet test CCInfoWindows/CCInfoWindows.Tests/CCInfoWindows.Tests.csproj` to confirm no regressions.

    If any of the above entries are missing or have incorrect values, add/fix them following the existing pattern (see ExportButton resw entries as reference). But based on research, all entries are already present and correct — expect zero code changes.
  </action>
  <verify>
    <automated>dotnet build CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits with 0 errors and 0 warnings related to resw/XAML
    - grep for `FooterRefreshButton` in MainView.xaml returns a line with `l:Uids.Uid`
    - grep for `FooterRefreshButton.*ToolTipService.ToolTip` in en-US/Resources.resw returns `Refresh`
    - grep for `FooterRefreshButton.*ToolTipService.ToolTip` in de-DE/Resources.resw returns `Aktualisieren`
    - grep for `FooterSettingsButton.*ToolTipService.ToolTip` in en-US/Resources.resw returns `Settings`
    - grep for `FooterQuitButton.*ToolTipService.ToolTip` in en-US/Resources.resw returns `Quit`
    - grep for `FooterRefreshButton.*AutomationProperties.Name` in en-US/Resources.resw returns `Refresh`
    - `dotnet test` passes with 0 failures
  </acceptance_criteria>
  <done>Project builds cleanly; all 3 footer buttons have l:Uids.Uid in XAML; all 12 resw entries (6 en-US + 6 de-DE) confirmed present with correct values; all existing tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Runtime tooltip and accessibility verification</name>
  <files>CCInfoWindows/CCInfoWindows/Views/MainView.xaml</files>
  <read_first>
    .planning/phases/15-footer-tooltip-accessibility/15-RESEARCH.md
  </read_first>
  <action>
    Launch the app with `dotnet run --project CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj` and present the user with the verification checklist below. The user must manually hover over each footer button and confirm tooltip text, then switch language and re-verify. No code changes — this is a human-only runtime verification gate.
  </action>
  <what-built>
    Footer button tooltips and screen reader accessibility — all implemented via WinUI3Localizer Uid property-path pattern in resw files. Three footer buttons (Refresh, Settings, Quit) should show localized tooltips on hover and announce their purpose to screen readers.
  </what-built>
  <how-to-verify>
    Start the app: `dotnet run --project CCInfoWindows/CCInfoWindows/CCInfoWindows.csproj`

    **ACC-01 — Tooltip hover test (en-US):**
    1. Ensure app language is set to English (Settings > Language > English)
    2. Hover mouse over the Refresh button (circular arrow icon, bottom center) — expect tooltip "Refresh"
    3. Hover mouse over the Settings button (gear icon, bottom center) — expect tooltip "Settings"
    4. Hover mouse over the Quit button (X icon, bottom center) — expect tooltip "Quit"

    **ACC-03 — Tooltip language switch test (de-DE):**
    5. Switch app language to German (Settings > Language > Deutsch)
    6. Hover Refresh button — expect tooltip "Aktualisieren"
    7. Hover Settings button — expect tooltip "Einstellungen"
    8. Hover Quit button — expect tooltip "Beenden"

    **ACC-02 — Accessibility test (optional, requires Accessibility Insights or Narrator):**
    9. Open Windows Narrator (Win+Ctrl+Enter) or Accessibility Insights for Windows
    10. Tab to each footer button — screen reader should announce "Refresh", "Settings", "Quit" (or German equivalents)
  </how-to-verify>
  <verify>
    <automated>MISSING — ACC-01/02/03 are inherently manual UI verifications requiring hover interaction and screen reader</automated>
  </verify>
  <resume-signal>Type "approved" if all tooltips appear correctly in both languages, or describe any issues found.</resume-signal>
  <acceptance_criteria>
    - All 3 footer buttons show correct English tooltips on hover
    - All 3 footer buttons show correct German tooltips on hover after language switch
    - Screen reader announces button purposes (if tested with Narrator/Accessibility Insights)
  </acceptance_criteria>
  <done>All three footer buttons display localized tooltips matching the current app language; screen reader announces button purposes. ACC-01, ACC-02, ACC-03 are satisfied.</done>
</task>

</tasks>

<verification>
Phase 15 is complete when:
1. Build compiles cleanly with no errors
2. All 12 resw entries (6 per language) are present and correct
3. XAML has correct l:Uids.Uid on all 3 footer buttons
4. User has visually confirmed tooltips appear on hover in both languages
5. User has confirmed screen reader announces button purposes (ACC-02)
</verification>

<success_criteria>
- Zero code changes needed (implementation already complete)
- Clean build with no regressions
- Human verification confirms tooltips work in en-US and de-DE
- AutomationProperties.Name verified via screen reader or Accessibility Insights
- All 3 requirements (ACC-01, ACC-02, ACC-03) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/15-footer-tooltip-accessibility/15-01-SUMMARY.md`
</output>
